{
  "$schema": "https://codi.works/harness/planning-manifest-schema.v1.json",
  "version": 1,
  "kind": "planning-manifest",
  "ownership": "planning-publisher-generated-snapshot",
  "generated": true,
  "description": "Immutable top-down planning delivery envelope. Human-owned planning source is never rewritten by validation or projection.",
  "requiredFields": ["schemaVersion", "manifestVersion", "projectId", "sourceRevision", "generatedAt", "digestAlgorithm", "digest", "needs", "screens", "features", "featureDetails", "flows", "relations", "decisions"],
  "digestAlgorithm": "sha256",
  "volatileDigestFields": ["generatedAt", "digest"]
}
