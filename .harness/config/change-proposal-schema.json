{
  "$schema": "https://codi.works/harness/change-proposal-schema.v1.json",
  "version": 1,
  "kind": "change-proposal",
  "ownership": "downstream-proposal-with-planning-disposition",
  "generated": false,
  "description": "Evidence-backed reverse proposal. It never updates either source automatically.",
  "requiredFields": ["id", "entity", "fieldPath", "plannedValue", "observedValue", "evidence", "rationale", "proposer", "decisionOwner", "status"],
  "statusValues": ["open", "accepted", "rejected", "deferred", "resolved"],
  "resolutionValues": ["implementation-fix", "planning-change", "defer", "reject"]
}
