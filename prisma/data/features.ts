// prisma/data/features.ts
// 실제 작업물(Feature) 데이터를 이 파일에서 관리합니다.
// 새 프로젝트 추가 시 아래 배열에 객체를 추가하세요.

// 프로젝트명 (제목)
// 간단한 설명 (1~2문장)
// 기술 스택 (예: NestJS, PostgreSQL, Redis...)
// 개발 기간 (예: 2024.07 ~ 2024.11)
// 역할/팀 구성 (예: 백엔드 단독, 또는 팀 2명)
// 프로젝트 개요 및 본문 (마크다운 가능, 문제 상황/접근 방법/결과 등)

export type SeedFeature = {
  slug: string;
  title: string;
  description: string;
  iconName: string;
  category: string;
  techStack: string[];
  status: string;
  apiCount: number;
  overview: string;
  period?: string | null;
  team?: string | null;
  content?: string | null;
};

export const REAL_FEATURES: SeedFeature[] = [
  {
    slug: 'the-siena-golf-reservation',
    title: '더시에나 골프 예약 시스템 구축',
    description:
      '외부 API 의존도가 높은 예약 시스템에서, 인메모리 버퍼링 기반의 파일 로깅 아키텍처를 도입하여 DB 병목을 해결하고 시스템 안정성을 확보한 프로젝트입니다.',
    iconName: 'CalendarCheck',
    category: 'Fullstack',
    techStack: ['PHP', 'MySQL'],
    status: 'Production',
    apiCount: 0,
    overview:
      '외부 파트너사의 예약 API에 전적으로 의존하는 구조에서, 사용자 예약 흐름과 외부 시스템 간의 안정적인 연계를 목표로 예약/마이페이지 기능 및 통신 로깅 아키텍처를 설계하고 구현했습니다.',
    period: '2023.05 – 2023.06',
    team: 'FE/BE 단독',
    content: `## 1. 프로젝트 개요
외부 파트너사의 예약 API에 전적으로 의존하는 구조에서, 사용자 예약 흐름과 외부 시스템 간의 안정적인 연계를 목표로 예약/마이페이지 기능 및 통신 로깅 아키텍처를 설계하고 구현했습니다.

## 2. 문제 상황

### 1) 외부 의존 구조에서의 리스크
예약 데이터가 전적으로 외부 API에 존재하여, 파트너사 서버의 응답 지연이나 타임아웃이 곧바로 사용자 경험 저하(내부 서버 오류로 인식)로 이어지는 구조적 리스크가 있었습니다.

### 2) 비정상 트래픽에 대한 검증 불가
호텔 행사 진행 시 파트너사로부터 "비정상적인 대량 요청이 발생했다"는 클레임이 접수되었으나, 당사 서버에서 발생한 실제 요청 건수나 네트워크 레벨의 재전송 여부를 증명할 객관적 데이터가 없었습니다.

### 3) DB 기반 로깅의 병목
초기에는 모든 cURL 통신 로그를 DB에 저장하도록 설계했으나, 수개월 동안 수백만 개의 로그 데이터가 쌓이면서 테이블이 비대해졌고, DB GUI 툴이 다운되는 현상까지 발생했습니다. 메인 비즈니스와 로깅이 동일한 DB 리소스를 공유하며 시스템 안정성을 해치는 역설적인 상황이었습니다.

## 3. 접근 전략

### 1) 외부 장애에 대한 UX 방어 설계
단순 예외 처리를 넘어 외부 API 실패를 사용자 경험 관점에서 재해석했습니다. 타임아웃을 "일시적 혼잡"으로 안내하고, 요청-응답 구간을 시각화하여 사용자의 혼란을 방지하고자 했습니다.

### 2) 로깅 아키텍처 분리 (DB → File)
DB는 핵심 비즈니스 데이터를 위한 자원으로 유지해야 한다고 판단했습니다. 쓰기 위주이며 트랜잭션 정합성이 덜 중요한 로그의 특성을 고려해, 비즈니스 DB와 로깅을 분리하는 전략을 세웠습니다.

### 3) Fail-Safe 설계
"로깅은 실패할 수 있지만, 예약은 실패하면 안 된다"는 대원칙을 세우고, 로그 기록 실패가 메인 비즈니스 로직에 영향을 주지 않도록 모듈을 철저히 격리하기로 했습니다.

## 4. 구체적 해결 과정

### 1) 중복 요청 방어
요청 직후 버튼을 비활성화하고 로딩 상태를 명확히 하여, 클라이언트 단에서 다중 클릭으로 인한 서버 단 중복 요청 가능성을 사전에 차단했습니다.

### 2) 인메모리 버퍼링 기반 cURL 로그 시스템 구축
DB 로깅을 제거하고 날짜 단위(YYYY-MM-DD) 파일 로그 시스템을 직접 구현했습니다. 특히 파일 I/O 병목을 방지하기 위해 **인메모리 버퍼링 패턴을 적용**했습니다. 단건마다 파일에 쓰는 대신, 배열에 로그를 적재하다가 **로그가 20건 쌓이거나 마지막 쓰기 이후 2초가 경과했을 때 일괄적으로 플러시** 하도록 구현하여 시스템 부하를 최소화했습니다.

### 3) 로깅 모듈 완전 격리
로깅 모듈 전체를 try-catch로 감싸, 파일 권한 오류나 디스크 용량 부족 문제가 발생하더라도 예약 흐름 자체에는 전혀 영향을 주지 않도록 구현했습니다.

## 5. 결과 및 영향

### 1) 데이터 기반 방어 체계 확보
구축한 파일 로그(요청 시각, 호출 IP, 세션 등)를 바탕으로, 행사 기간 중 발생한 비정상 트래픽이 당사 홈페이지를 통한 정상적인 API 호출 패턴이 아님을 증빙했습니다. 이를 통해 파트너사의 클레임을 객관적 수치로 방어하고 협업 관계를 안정화했습니다.

### 2) 시스템 안정성 개선
로깅 아키텍처 변경 및 버퍼링 도입으로 메인 DB의 병목 현상과 GUI 다운 문제를 완전히 해결하였고, 외부 장애 발생 시에도 유연한 운영 대응이 가능해졌습니다.

## 6. 배운 점 및 확장 사고

### 1) 로그 라이프사이클 관리의 중요성
당시에는 SSH 접속 후 수동 삭제 방식으로 로그를 관리했습니다. 현재 동일 구조를 설계한다면 Logrotate 기반의 자동 파기 정책, 로그 레벨 분리(INFO/WARN/ERROR), 중앙 집중형 로그 수집 아키텍처(ELK, Datadog 등)를 고려해 운영 확장성까지 포함한 설계를 진행했을 것입니다.

### 2) 동시성 제어에 대한 재설계 (현재 관점)
당시에는 "실제 데이터는 외부 시스템에 있으므로 동시성 문제는 파트너사의 책임"이라는 전제를 두었습니다. 현재라면 클라이언트 진입 시점부터 **Redis 기반 TTL Lock (3~5분)** 을 적용할 것입니다. 선점 실패 시 사용자에게 즉시 안내하고 확정 시 Lock을 해제하는 방식으로, 동시성 제어를 외부에 위임하지 않고 우리 시스템 내에서 주도적으로 제어하는 아키텍처를 그릴 것입니다.`,
  },

  {
    slug: 'hanmaum-science-institute',
    title: '한마음과학원',
    description: '77만 자의 비정형 데이터를 파싱하여 DB화하고, MySQL 인덱스와 하이브리드 라우팅을 결합해 대용량 검색 응답 속도를 1500ms에서 400ms로 73% 개선한 프로젝트입니다.',
    iconName: 'Search',
    category: 'Fullstack',
    techStack: ['JavaScript', 'React', 'Node.js', 'Express', 'MySQL', 'Sequelize', 'GitHub Actions', 'PHP'],
    status: 'Production',
    apiCount: 0,
    overview: '77만 자의 비정형 데이터를 파싱하여 DB화하고, MySQL 인덱스와 하이브리드 라우팅을 결합해 대용량 검색 응답 속도를 1500ms에서 400ms로 73% 개선한 프로젝트입니다.',
    period: '2023.06 – 2023.10',
    team: 'FE 2명 (본인 포함), BE 1명 (본인)',
    content: `## 1. 프로젝트 개요
Word 기반의 교재 데이터를 웹 서비스로 이관하고, 대용량 텍스트 검색 기능과 관리자 업로드 시스템을 구축한 프로젝트입니다. 1인 백엔드 개발자로서 교재 데이터 파싱 아키텍처 설계, RDBMS 기반 검색 최적화, 인증 로직 구현, CI/CD 배포 환경 구성까지 서버사이드 전반을 주도했습니다.

## 2. 문제 상황
### **1) 비정형 대용량 데이터의 DB화 과제**
교재가 오직 Word 파일 형태로만 존재했으며, 이를 구조화하여 DB에 적재해야 했습니다. 최대 454페이지, 약 77만 자에 달하는 텍스트를 파싱하여 Insert 하는 과정에서 인스턴스 서버의 부하 및 데이터 무결성 훼손 리스크가 존재했습니다.
### **2) 검색 성능 및 정확도의 한계**
단락 단위의 텍스트 검색 기능이 필수적이었으나, 기존 검색 방식은 응답 시간이 1500ms에 달해 UX를 크게 저해했습니다. 제한된 일정과 리소스 내에서 Elasticsearch 같은 전문 검색 엔진 도입 없이 빠르고 정확한 검색을 구현해야 했습니다.
### **3) 수동 배포로 인한 운영 리스크**
수동 배포 과정에서 휴먼 에러 발생 가능성이 컸으며, 프로젝트 릴리즈 이후의 운영 안정성을 보장할 파이프라인이 부재했습니다.

## 3. 접근 전략
### **1) 정규식 기반 데이터 파싱 및 로컬 마이그레이션**
Word 파일을 HTML로 변환 후 정규식 기반으로 구조화하는 전략을 세웠습니다. 특히 인스턴스 서버의 스펙 한계로 인한 다운타임을 방지하기 위해, 대용량 파싱 및 DB 적재 프로세스를 로컬 스크립트로 분리하여 실행하는 현실적인 마이그레이션 방안을 채택했습니다.
### **2) 하이브리드 검색 라우팅 (MySQL FULLTEXT + LIKE)**
MySQL InnoDB의 FULLTEXT 기능을 최대한 활용하되, 최소 토큰 길이(\`innodb_ft_min_token_size\`) 제약을 백엔드 애플리케이션 레벨의 로직으로 보완하는 하이브리드 검색 아키텍처를 설계했습니다.
### **3) 운영 및 보안 안정성 확보**
토큰 기반(Access/Refresh) 인증 및 쿠키 보안 설정을 적용하고, GitHub Actions를 활용해 무중단 자동 배포 환경을 구축하여 운영 효율성을 높였습니다.

## 4. 구체적 해결 과정
### **1) 3단계 정규식 패턴을 활용한 대용량 데이터 파싱**
- \`mammoth\` 라이브러리를 활용해 Word 문서를 HTML로 변환했습니다.
- 고객사가 1차 분류한 태그 형태(\`[xx-1-1-1]\`, \`[xx-1-1]\`, \`[xxxx-1]\`)를 분석하여 **3가지 핵심 정규식 패턴**을 도출해 냈고, 이를 기준으로 카테고리, 볼륨(Vol), 단락을 도메인 식별자로 분리했습니다.
- 예외적인 비정형 텍스트(약 2~3천 단어)는 파싱 로직을 과도하게 복잡하게 만드는 대신 클라이언트와 협의하여 수동 처리하는 유연함을 발휘했습니다.
- 파일당 77만 자 분량의 데이터를 로컬 환경에서 건당 3분 이내에 파싱 및 적재 완료하였으며, \`Sequelize\` 트랜잭션을 적용해 실패 시 Rollback 되도록 하여 데이터 무결성을 확보했습니다.
### **2) 백엔드 검색 라우팅 구현 및 정렬 로직 최적화**
- Node.js 백엔드 단에서 검색어 길이를 동적으로 계산하는 분기 로직을 직접 구현했습니다. **3글자 이상은 FULLTEXT BOOLEAN MODE로, 2글자 이하의 단어는 LIKE 검색으로 라우팅**되도록 처리했습니다.
- 정확히 일치하는 단락에 최고 가중치를 부여하고, 인접 단락 간의 비교 로직을 추가하여 문맥 기반의 정렬 알고리즘을 구현했습니다.
### **3) 인증 설계 및 CI/CD 자동화 구축**
- Access / Refresh Token 구조를 채택하고, \`httpOnly\`와 \`secure\` 옵션을 적용해 XSS 공격을 방어했습니다. 토큰에는 최소한의 Payload(UUID)만 담고 사용자 정보는 별도 API로 조회하도록 설계했습니다.
- \`master\` 브랜치 Merge 시 GitHub Actions가 트리거되어 SSH 공개키 인증 방식으로 서버에 자동 배포되도록 구성했습니다.

## 5. 결과 및 영향
- **검색 성능 73% 향상 (1500ms → 400ms):** 하이브리드 검색 라우팅 설계 적용 후, 검색되는 데이터의 양과 정확도가 상승했음에도 불구하고 응답 시간은 400ms 수준으로 대폭 단축되었습니다.
- **안정적인 데이터 이관 완수:** 총 5개의 대용량 교재 파일을 데이터 유실이나 서버 장애 없이 성공적으로 서비스 DB에 안착시켰습니다.
- **운영 효율성 증대:** 수동 배포 제거를 통해 백엔드 인프라 운영의 안정성을 크게 높였습니다.

## 6. 배운 점 및 확장 사고 (Insight)
이 프로젝트를 통해 백엔드 설계에 대한 시야를 한 단계 확장할 수 있었습니다.
- **검색 설계에 대한 인사이트:** RDBMS FULLTEXT 기반의 하이브리드 검색으로 성과를 냈으나, 현재 동일한 요구사항을 마주한다면 형태소 분석기(Nori)를 연동한 Elasticsearch 도입을 1순위로 고려할 것입니다. BM25 기반의 스코어링 방식과 검색 로그를 활용한 Relevance 튜닝을 통해 더 전문적인 검색 엔진 아키텍처를 설계해 보고 싶습니다.
- **보안 설계 관점의 성숙:** 당시 쿠키 기반 인증을 구현하며 XSS는 방어했으나 CSRF 취약점에 대한 고려는 부족했습니다. 보안 설계는 단순한 "구현"이 아니라 "공격 벡터를 가정하는 방어적 사고"가 핵심임을 깨달았습니다. 현재라면 SameSite 정책 명확화, Double Submit Cookie 방식 적용, Refresh Token Rotation 도입을 통해 더 견고한 인증 체계를 구축할 것입니다.
- **배포 구조 및 데이터 처리 인프라 고도화:** 인스턴스 서버 부하를 우려해 로컬 마이그레이션을 선택한 것은 현실적인 대응이었으나, 현재라면 Node.js 비동기 워커 분리 또는 청크 단위 스트리밍 처리를 통해 서버 자체에서 대용량 데이터를 안전하게 소화하도록 설계할 것입니다. 또한, 단순 직배포 구조를 넘어 Docker 기반의 배포나 롤백 전략이 포함된 CI/CD로 파이프라인을 고도화할 것입니다.`,
  },
  {
    slug: 'blackstone-belleforet-resort',
    title: '블랙스톤 벨포레리조트 웹사이트 리뉴얼 및 예약 시스템 구축',
    description: '단순 기능 구현을 넘어 파트너사와의 API 규격을 주도적으로 설계했으며, 클라이언트 단의 보안 취약점을 인지하고 프록시를 거쳐 BFF 아키텍처의 필요성을 체감하며 구조를 개선한 프로잭트 입니다.',
    iconName: 'Laptop',
    category: 'Fullstack',
    techStack: ['JavaScript', 'React', 'Node.js', 'Express', 'MySQL', 'Sequelize', 'Jenkins', 'PHP'],
    status: 'Production',
    apiCount: 0,
    overview: '기존 PHP 중심으로 운영되던 리조트 웹사이트에 React를 도입하여, 예약/로그인/마이페이지 등 사용자 인터랙션이 많은 핵심 구간의 UX를 동적으로 개선한 프로젝트입니다.',
    period: '2023.12 – 2024.10',
    team: 'FE 3명 (본인 포함) / BE 1명 (본인)',
    content: `## 1. 프로젝트 개요
기존 PHP 중심으로 운영되던 리조트 웹사이트에 React를 도입하여, 예약/로그인/마이페이지 등 사용자 인터랙션이 많은 핵심 구간의 UX를 동적으로 개선한 프로젝트입니다. 기존 조직의 기술 스택을 전면 부정하고 새로 엎는 대신, 현실적인 일정과 협업 환경을 고려하여 **PHP 레거시와 React, 그리고 Node.js가 함께 동작하는 '점진적 공존 아키텍처**'를 설계하고 구현했습니다.

## 2. 직면한 문제 상황

### **1) 기존 기술 스택(PHP)과의 아키텍처 충돌**
회사의 기존 인프라 및 퍼블리셔 협업 체계가 철저히 PHP 라우팅에 맞춰져 있었습니다. React 도입 시 서버 사이드 라우팅과 SPA 클라이언트 라우팅 간의 충돌을 방지하고 도메인 구조를 명확히 분리해야 했습니다.

### **2) 크로스 플랫폼(WebView) 환경의 세션 유실**
PC와 모바일 웹 브라우저에서는 정상 유지되던 JWT 쿠키 기반의 로그인 세션이, 앱(WebView) 환경에서만 반복적으로 풀리는 현상이 발생했습니다.

### **3) 분리된 결제 로직으로 인한 데이터 원자성 결여**
결제 승인 ➝ DB 저장 ➝ 외부 API 호출 로직이 물리적으로 파편화되어 실행되고 있었습니다. 이로 인해 DB에는 예약 데이터가 생성되었으나 실제 결제 처리는 누락되는 간헐적 '데이터 불일치'가 발생하여 시스템 신뢰도를 떨어뜨렸습니다.

## 3. 접근 전략

### **1) 전면 교체가 아닌 공존과 실험 (Polyglot Architecture)**
기존 서버 구조를 존중하여 별도 서브도메인 분리 대신 동일 도메인 하위 Path 전략을 채택했습니다. 특히 NICE 본인인증 모듈은 제공된 공식 샘플 코드를 활용할 겸, 기존 PHP 구조에 Node.js(Express) 마이크로서비스를 결합해도 안정적인 통신이 가능한지 아키텍처적 검증을 시도했습니다.

### **2) 결제의 단일 흐름화 및 보상 트랜잭션 (Compensating Transaction)**
결제는 여러 로직의 파편이 아니라 하나의 흐름으로 묶여야 한다고 정의했습니다. 실패 지점을 명확히 통제하여 데이터 정합성을 강제하는 방어적 설계를 목표로 삼았습니다.

### **3) 수동적 구현자를 넘어선 'API 공동 설계자'**
파트너사에도 전례가 없던 신규 티켓 기능을 개발하기 위해, 단순히 API 명세를 기다리는 대신 프론트엔드 요구사항에 최적화된 Response 구조를 먼저 제안하며 기능 흐름을 주도적으로 설계했습니다.

## 4. 구체적 해결 과정

### **1) Nginx 기반의 유연한 SPA 공존 라우팅 설계**
\`/app/*\` 경로로 들어오는 요청은 Nginx에서 React 빌드 파일로 분기하도록 설정하고, 직접 URL 접근 시 \`index.html\`로 Fallback 되도록 처리했습니다. 이를 통해 Same-Origin을 유지하며 PHP와 React가 충돌 없이 완벽히 공존하는 환경을 구축했습니다.

### **2) WebView 브릿지 통신 규격 정립**
앱과 웹 브라우저의 스토리지 및 쿠키 정책 차이를 파악한 후, 앱 개발자와 협업하여 브릿지 인터페이스를 구축했습니다. 웹에서 로그인 성공 후 발급된 토큰을 \`window.{합의된_함수명}.postMessage\` 방식으로 네이티브 앱에 안전하게 전달하여 WebView 로그인 풀림 이슈를 해결했습니다.

### **3) 에러 방어 및 UX 중심의 결제 플로우 통합**
결제 승인부터 외부 API 연동까지의 과정을 단일 흐름으로 통합했습니다. 만약 외부 API 호출 단계에서 네트워크 에러가 발생하면, \`tid\`의 존재 유무를 확인해 **즉시 PG사 결제 취소 API를 호출하는 보상 트랜잭션**을 태웠습니다. 동시에, 사용자가 입력했던 폼 데이터를 스토리지에서 다시 꺼내어 채워주는 로직을 구현하여 결제 실패 시 겪는 사용자의 불편함을 최소화했습니다.

## 5. 결과 및 영향

- **성공적인 아키텍처 공존:** 레거시(PHP)와 모던 스택(React, Node.js)을 안정적으로 결합하여 조직 내 새로운 기술 도입의 성공적인 레퍼런스를 만들었습니다.
- **결제 데이터 무결성 확보:** 보상 트랜잭션과 로직 통합을 통해 결제 데이터 불일치 이슈를 완벽히 제거했습니다.
- **주도적 협업 경험:** 앱 개발자, 외부 파트너사와의 브릿지 통신 및 API 규격을 주도적으로 설계하며 프로젝트의 완성도를 크게 높였습니다.

## 6. 배운 점 및 확장 사고 (Insight)

- **결제는 상태관리가 핵심이다:** 결제는 단방향 로직이 아니라 여러 단계의 상태 전이를 갖는 프로세스임을 체감했습니다. 향후 시스템을 설계한다면 \`INIT\` ➝ \`APPROVED\` ➝ \`CONFIRMED\` ➝ \`FAILED\` 등 명확한 상태 머신 기반으로 결제 파이프라인을 재설계하여 트랜잭션 안정성을 극대화할 것입니다.
- **플랫폼 환경의 다름을 이해하는 시야:** 웹과 WebView 앱 환경의 저장 구조 및 통신 방식 차이를 직접 해결하며, 단순 브라우저를 넘어 크로스 플랫폼 환경의 인프라적 특징을 이해하는 시야를 갖추게 되었습니다.
- **계약을 주도하는 개발자로의 성장:** API는 단순한 데이터 요청이 아니라 시스템 간의 엄격한 계약임을 배웠습니다. 주어진 명세를 코드로 옮기는 구현자를 넘어, 협업 구조와 데이터 계약을 기획 단계부터 주도적으로 설계하는 아키텍트의 관점을 가지게 된 뜻깊은 프로젝트였습니다.
- 초기 React 도입 시 클라이언트에서 외부 API를 직접 호출하여 브라우저 네트워크 탭에 API Key가 노출되는 보안 이슈를 겪었습니다. 즉시 PHP 기반의 프록시 서버를 구축해 통신을 우회하는 핫픽스를 적용했습니다. 이 시행착오를 통해 SPA 환경에서는 클라이언트와 외부 시스템 사이에 **보안 통제와 데이터 정제를 전담하는 BFF(Backend For Fe) 계층이 필수적**임을 뼈저리게 배웠으며, 향후에는 외부 API 응답을 UI에 맞게 매핑하여 전달하는 더 견고한 아키텍처를 설계하고자 합니다.`
  },
  {
    slug: 'integrated-sso-server',
    title: '통합 회원 관리 인증 서버 (SSO) 설계 및 구축',
    description: '사내 서비스들의 분산된 인증 체계를 통합하기 위해 NestJS 기반으로 구축한 중앙 집중형 SSO 서버로, JWT Payload와 Soft FK를 활용해 물리적으로 분리된 DB 환경에서 안전한 신뢰 모델을 설계했습니다.',
    iconName: 'ShieldCheck',
    category: 'Backend',
    techStack: ['TypeScript', 'NestJS', 'MySQL', 'Prisma', 'Jenkins', 'PM2'],
    status: 'Production',
    apiCount: 0,
    overview: '사내 여러 서비스에 파편화되어 있던 인증 체계를 하나로 통합하기 위해, TypeScript와 NestJS 기반의 중앙 집중형 통합 회원 관리 서버를 단독으로 설계하고 구축했습니다. 단순한 로그인 API 모음을 넘어, 서비스 간 신뢰 모델을 보장하는 Provider 기반의 확장 가능한 인증 플랫폼을 만드는 것을 목표로 진행했습니다.',
    period: '2024.07 – 2024.12',
    team: 'BE 1명 (본인)',
    content: `### 1. 프로젝트 개요
사내 여러 서비스에 파편화되어 있던 인증 체계를 하나로 통합하기 위해, TypeScript와 NestJS 기반의 중앙 집중형 통합 회원 관리 서버를 단독으로 설계하고 구축했습니다. 단순한 로그인 API 모음을 넘어, 서비스 간 신뢰 모델을 보장하는 **Provider 기반의 확장 가능한 인증 플랫폼**을 만드는 것을 목표로 진행했습니다.

### 2. 직면한 문제 상황

**1) 사내 인증 체계의 파편화 및 보안 취약성**
각 사내 서비스가 독립적으로 회원을 관리하면서 토큰 정책(만료 시간 등) 불일치, 보안 수준의 차이, 인증 Key 관리의 혼재 등 운영 및 보안상의 리스크가 존재했습니다.

**2) 분산 DB 환경에서의 데이터 식별 문제**
회원 정보는 통합 서버에서 관리하지만, 실제 비즈니스 도메인(주문, 예약 등) 데이터는 개별 서비스 DB에서 관리해야 했습니다. 물리적으로 분리된 DB 환경에서 식별자(FK)를 어떻게 설계하고 장애를 추적할지에 대한 아키텍처적 고민이 필요했습니다.

**3) Server-to-Server 신뢰 모델 부재**
단순한 클라이언트 인증을 넘어, 중앙 서버와 각 도메인 서비스 간의 권한과 인증 상태를 안전하게 검증할 신뢰 아키텍처가 부재했습니다.

### 3. 접근 전략

**1) JWT Payload와 Soft FK를 활용한 느슨한 결합**
물리적 DB 분리로 인한 제약을 극복하기 위해, 통합 서버에서 고유한 식별자(UUID)를 발급하여 Access Token의 Payload에 담아 전달하는 방식을 채택했습니다. 개별 서비스는 이 값을 추출해 자신들의 DB에 **Soft FK**로 저장함으로써 데이터 정합성과 독립성을 동시에 확보했습니다.

**2) Provider 기반 중앙 집중형 인증 플랫폼 설계**
개별 서비스별로 JWT Secret 분리, Authorization Key 발급, 허용 IP 관리를 적용했습니다. Access Token은 각 서비스가 자체 검증하여 부하를 분산하되, **Refresh Token은 반드시 통합 서버를 통해서만 검증**하도록 하여 중앙 통제력을 유지했습니다.

**3) NestJS 생태계 도입을 통한 아키텍처 고도화**
기존 Express 구조의 한계를 넘어, 객체지향적이고 레이어가 명확히 분리된 백엔드 설계를 경험하고자 NestJS와 TypeScript를 선제적으로 도입했습니다.

### 4. 구체적 해결 과정

**1) NestJS 기반의 선언적 인증/인가 파이프라인 구축**
- **Guard 및 Decorator 활용:** 토큰의 유효성 검증과 Provider 권한 인가 로직을 NestJS의 \`@UseGuards\`로 분리하여 비즈니스 로직과 격리했습니다. 또한, Custom Decorator를 작성하여 서버로 인입되는 Request Body와 Param의 유효성 검사를 선언적으로 처리해 코드 가독성과 안정성을 높였습니다.
- **DI(Dependency Injection) 적용:** 모듈 간 결합도를 낮추고 테스트 및 확장이 용이한 구조로 리팩토링했습니다. 이 과정에서 AI 도구를 보조로 활용했으나, 로직 누락 방지 및 최종 보안 검증은 개발자가 직접 주도하여 시스템의 신뢰도를 확보했습니다.

**2) Prisma 기반 통제형 토큰 데이터베이스 설계**
- Access Token은 10분, Refresh Token은 7일(자동 로그인 시 365일)로 정책을 수립했습니다.
- 특히 Refresh Token을 Prisma를 통해 DB에 저장 및 추적하도록 설계하여, 필요시 DB 레코드 삭제만으로 특정 유저나 디바이스를 **강제 로그아웃**시킬 수 있는 강력한 통제 환경을 구현했습니다.

### 5. 결과 및 영향

- **사내 인증 보안 및 정책 표준화:** 분산되어 있던 모든 사내 서비스의 인증 로직을 중앙화하여 보안 수준을 상향 평준화했습니다.
- **운영 통제력 강화:** 강제 로그아웃 기능과 IP 화이트리스트 검증을 통해 악의적인 접근이나 세션 탈취에 즉각적으로 대응할 수 있는 기반을 마련했습니다.
- **배포 파이프라인 안정화:** Jenkins를 활용한 CI/CD 배포 자동화를 구축하여 1인 백엔드 체제에서의 운영 리소스를 대폭 절감했습니다.

### 6. 배운 점 및 확장 사고 (Insight)

**“인증은 단순한 문자열 발급이 아니라, 시스템 간의 신뢰 설계다.”**

이 프로젝트를 통해 분산 아키텍처 환경에서 토큰과 식별자를 어떻게 안전하게 주고받아야 하는지 깊이 체감했습니다. 현재의 안정적인 구조에 머무르지 않고, 향후 트래픽 증가와 보안 고도화를 위해 다음과 같은 확장을 구상하고 있습니다.

- **인프라 병목 방지 및 스케일아웃:** 현재 Refresh Token 검증 시 발생하는 DB 조회 트래픽은 단일 인스턴스로 충분히 소화 가능하지만, 향후 대규모 트래픽 발생 시 PM2 Cluster 모드를 활용한 인스턴스 스케일아웃과 더불어 **Redis를 캐싱 레이어로 도입 (DB + Redis)**하여 RDBMS의 I/O 부하를 근본적으로 줄이는 설계를 고려하고 있습니다.
- **보안 토큰 라이프사이클 고도화:** 보안의 완결성을 높이기 위해 Refresh Token Rotation(갱신 시마다 새로운 토큰 발급) 및 Token Reuse Detection(탈취된 토큰의 재사용 감지 시 모든 세션 폐기) 로직, 그리고 Key Rotation 전략과 감사 로그(Audit Log) 시스템을 추가로 도입해 보고 싶습니다.`
  },
  {
    slug: 'hipass-b2b-platform',
    title: '하이패스 (화훼 도소매 B2B 주문 플랫폼)',
    description: '1인 백엔드로서 결제/정산 자동화 파이프라인을 단독 설계하고, 프론트엔드의 기능 단위 아키텍처 표준화까지 주도하여 시스템 전체의 안정성과 유지보수성을 끌어올린 프로잭트 입니다.',
    iconName: 'Store',
    category: 'Fullstack',
    techStack: ['JavaScript', 'React', 'Node.js', 'Express', 'MySQL', 'Sequelize', 'Jenkins', 'Socket.io'],
    status: 'Production',
    apiCount: 0,
    overview: '도매상과 화원 간의 실시간 화훼 거래를 지원하는 B2B 플랫폼 구축 프로젝트입니다. 1인 백엔드 개발자로서 실시간 주문 상태 동기화, 구독 기반 자동 빌링(Toss 연동), 정산 파이프라인을 단독 설계했습니다. 또한, 전체적인 코드 품질 향상을 위해 프론트엔드의 초기 아키텍처와 폴더 구조 표준화를 주도적으로 리드했습니다.',
    period: '2024.08 – 2025.08',
    team: 'FE 2명 (본인 포함) / BE 1명 (본인)',
    content: `## 1. 프로젝트 개요
도매상과 화원 간의 실시간 화훼 거래를 지원하는 B2B 플랫폼 구축 프로젝트입니다. 1인 백엔드 개발자로서 실시간 주문 상태 동기화, 구독 기반 자동 빌링(Toss 연동), 정산 파이프라인을 단독 설계했습니다. 또한, 전체적인 코드 품질 향상을 위해 프론트엔드의 초기 아키텍처와 폴더 구조 표준화를 주도적으로 리드했습니다.

## 2. 직면한 문제 상황

### **1) 양방향 실시간 동기화의 필요성**
도매상과 화원이 주문 접수 ➝ 승인 ➝ 진행 ➝ 완료 상태를 즉각적으로 공유해야 했습니다. 기존의 HTTP Polling 방식은 트래픽 낭비와 서버 부하를 유발하므로 B2B 거래의 실시간성을 보장하기에 비효율적이었습니다.

### **2) 정산의 비가역성과 금융 데이터 보호**
외부 지급대행 API를 통한 정산은 한 번 실행되면 되돌릴 수 없는 비가역적 성격을 띕니다. 공휴일이나 네트워크 장애 시 중복 정산이 되거나 데이터가 누락되는 치명적인 사고를 방어해야 했고, 대량의 정산 데이터를 매번 DB에서 조회할 때 발생하는 부하 문제도 해결해야 했습니다.

### **3) 프론트엔드 아키텍처의 파편화 (과거의 교훈)**
과거 프로젝트에서 정형화된 아키텍처 부재로 인해 작업자마다 코드 스타일과 디렉터리 구조가 달라져 유지보수에 큰 어려움을 겪었던 한계가 있었습니다.

## 3. 접근 전략

### **1) WebSocket 기반 실시간 상태 동기화망 구축**
비효율적인 Polling을 걷어내고, Socket.io의 Room(User UUID 기반)을 활용해 상태 변경 시점에만 타겟 유저에게 이벤트를 Emit 하는 효율적인 실시간 통신망을 구축하기로 결정했습니다.

### **2) 간이 Outbox 패턴 및 서버 통제형 결제 설계**
DB 조회 부하와 정산 데이터 유실을 막기 위해 물리적 JSON 파일을 활용한 '간이 Outbox 패턴'을 고안했습니다. 또한, 결제는 클라이언트 콜백 의존성을 제거하고 서버에서 외부 API를 직접 호출하는 강력한 트랜잭션으로 묶어 정합성을 보장하는 전략을 세웠습니다.

### **3) 프론트엔드 기능(Feature) 중심 아키텍처 표준화**
과거의 산발적인 구조를 탈피하고자, 전역 상태를 '유저 정보' 수준으로 최소화하고 기능 단위로 폴더링을 모듈화하는 디렉터리 아키텍처 규약을 선제적으로 제시하여 프론트엔드의 유지보수성을 끌어올리기로 했습니다.

## 4. 구체적 해결 과정

### **1) Socket.io Room 기반 이벤트 파이프라인 구현**
주문 접수부터 완료까지의 상태 전이를 Socket.io Room 시스템으로 분리하여, 관련 도매상과 화원에게만 상태 변경 이벤트를 즉각적으로 Emit 하도록 구현해 서버 부하를 최소화했습니다.

### **2) JSON 파일 기반 정산 내구성 확보 및 스케줄러 분리**
정산 시점(월 2회)에 \`DB 상태 변경 ➝ 정산 대상 JSON 파일 생성 ➝ 지급 API 호출 ➝ 성공 시 파일 삭제\`의 프로세스를 적용했습니다. 실패 시 JSON 파일을 물리적으로 보존해 다음 날 안전하게 재시도하도록 구현했습니다. 스케줄러(\`node-schedule\`)는 PM2 단일 프로세스로 격리해 중복 실행을 막았으며, 향후 트래픽 증가 시 인스턴스 물리적 분리가 가능하도록 설계했습니다.

### **3) 결제 트랜잭션 보장 및 FE 캡슐화 적용**
\`user_subscription\` 테이블을 통해 구독을 관리하고, \`주문 생성 ➝ 결제 요청 ➝ 승인/상태 변경 ➝ 실패 시 취소\`의 흐름을 단일 트랜잭션으로 묶어 결제 불일치 한계를 100% 극복했습니다. 프론트엔드는 제안한 표준 아키텍처에 맞춰 각 기능 내에서 철저히 상태를 관리하도록 개발을 리드했습니다.

## 5. 결과 및 영향

- **결제/정산 데이터 불일치 0건 달성:** 서버 통제형 트랜잭션과 JSON Fallback 시스템을 통해 운영 기간 내내 단 한 건의 금융 데이터 오류도 발생하지 않는 높은 신뢰도를 확보했습니다.
- **조직 내 개발 표준 확립:** 주도적으로 설계한 프론트엔드 기능 단위 아키텍처가 팀 내 표준으로 자리 잡아, UI/UX 개발 속도와 코드 가독성이 비약적으로 상승했습니다.

## 6. 배운 점 및 확장 사고 (Insight)

- **결제 트랜잭션과 멱등성 고도화:** 결제 불일치 방지에는 성공했으나, 네트워크 지연에 따른 이중 결제 시도를 원천 차단하기 위해 **Idempotency Key(멱등성 키)**와 DB Unique 제약조건을 활용한 방어 로직을 추가로 도입해야 함을 체감했습니다. 잔액 부족 등 구독 실패 시 즉시 해지가 아닌 Grace Period(유예 기간)를 두는 비즈니스 로직 확장도 구상 중입니다.
- **DB 기반 Outbox와 MQ 도입:** 현재의 JSON 파일 기반 간이 Outbox는 DB I/O 부하 방지와 물리적 백업 측면에서 훌륭한 타협점이었습니다. 트래픽이 더욱 거대해지는 다음 단계에서는 이를 **DB 기반의 정규 Outbox 테이블**로 승격시키고, 메인 트랜잭션과 이벤트 발행을 분리하는 Message Queue(예: RabbitMQ, Kafka) 아키텍처로 고도화하여 결합도를 한층 더 낮추고 싶습니다.
- **실시간 통신의 신뢰성 (Socket ACK):** 현재의 Emit 구조에 더해 클라이언트의 수신을 보장하는 ACK(Acknowledgement) 구현과 서버 재시작 시의 메시지 복구 전략을 수립하여 실시간 시스템의 신뢰성을 극대화하고자 합니다.`
  },
  {
    slug: 'hotel-reservation-platform',
    title: '호텔 예약 시스템 플랫폼화 및 구조 고도화',
    description: '\'설정 주도(Config-driven) 아키텍처\'로 전면 재설계하여, 코드 수정 없이 다수의 신규 프로젝트 확장이 가능한 프론트엔드 플랫폼 환경을 구축한 프로젝트입니다.',
    iconName: 'LayoutTemplate',
    category: 'Fe',
    techStack: ['JavaScript', 'React', 'PHP', 'MySQL', 'Sequelize', 'Jenkins'],
    status: 'Production',
    apiCount: 0,
    overview: '단일 호텔에 종속되어 있던 기존의 예약 페이지를, 다수의 프로젝트(호텔)에 유연하게 복제 및 적용할 수 있는 플랫폼 아키텍처로 전환한 프로젝트입니다. 각기 다른 비즈니스 요구사항을 소스 코드 수정 없이 수용하기 위해 기능 ON/OFF 기반의 설정 주도 아키텍처를 프론트엔드에 설계 및 도입하여, 프론트엔드 파편화를 막고 유지보수 비용을 획기적으로 낮췄습니다.',
    period: '2024.11 – 2025.10',
    team: 'FE 2명 (본인 포함) / BE 1명',
    content: `## 1. 프로젝트 개요
단일 호텔에 종속되어 있던 기존의 예약 페이지를, 다수의 프로젝트(호텔)에 유연하게 복제 및 적용할 수 있는 **플랫폼 아키텍처**로 전환한 프로젝트입니다. 각기 다른 비즈니스 요구사항을 소스 코드 수정 없이 수용하기 위해 **기능 ON/OFF 기반의 설정 주도 아키텍처**를 프론트엔드에 설계 및 도입하여, 프론트엔드 파편화를 막고 유지보수 비용을 획기적으로 낮췄습니다.

## 2. 직면한 문제 상황

### **1) 하드코딩된 복제 구조의 기술 부채**
결제 방식, 복합 예약(호텔+골프), 다국어, 회원 정책 등 프로젝트마다 다른 요구사항을 공통 코드 내의 \`if-else\` 분기로 처리하다 보니, 비즈니스가 확장될수록 코드 복잡도가 기하급수적으로 증가했습니다.

### **2) 비대해진 예약 상태 관리 (Provider Hell & Props Drilling)**
\`객실 선택 ➝ 정보 입력 ➝ 결제\`로 이어지는 복잡한 상태 값들이 파편화되어 있었고, 이를 최상단에서 관리하려다 보니 Props Drilling과 전역 상태의 비대화 현상이 발생하여 비정상적인 접근에 취약해졌습니다.

### **3) 크로스 플랫폼 결제 UX 및 정합성 리스크**
모바일 환경은 PC(모달 방식)와 달리 페이지 이동 기반으로 결제가 진행되어, PG사 결제 화면에서 복귀 시 사용자의 입력 데이터가 유실되거나 그사이 재고/가격이 변동되어 결제 데이터가 불일치하는 심각한 리스크가 존재했습니다.

## 3. 접근 전략

### **1) Config-driven Architecture (설정 주도 아키텍처)**
코드를 수정하여 프로젝트별 분기를 태우는 대신, 관리자 페이지에서 프로젝트별 Feature를 ON/OFF 하면 **프론트엔드가 Initial Load 시점에 설정값을 주입받아 조건부 렌더링을 수행**하도록 아키텍처를 뒤집었습니다.

### **2) 라우터 레벨의 Context 분리와 캡슐화**
무거운 전역 상태 라이브러리 Redux 대신 React Context API를 채택하되, \`App.tsx\` 최상단에 모든 Provider를 몰아넣는 'Provider 지옥'을 피하기 위해 **라우터 레벨에서 도메인 단위(예약, 결제 등)로 Provider를 격리**하는 구조를 채택했습니다.

### **3) 로컬 캐싱과 서버 검증의 하이브리드 결제 구조**
모바일 결제 이탈 시 UX를 보호하기 위해 사용자의 순수 입력값은 \`localStorage\`에 캐싱하되, 가격이나 재고 같은 크리티컬한 비즈니스 데이터는 복귀 시 무조건 서버 API를 통해 재조회하여 정합성을 보장했습니다.

## 4. 구체적 해결 과정

### **1) Initial Load 블로킹 및 전역 Config 주입**
앱이 처음 로드될 때 API를 통해 설정값(Config)을 한 번에 가져와 최상단 \`Config Context\`에 올렸습니다. 데이터 무결성을 위해 렌더링을 일시 블로킹하되, 로딩 스피너를 배치하여 UX 저하를 최소화했습니다. 이를 통해 하위의 모든 컴포넌트가 안정적으로 Config 기반 분기 처리를 수행할 수 있었습니다.

### **2) Custom Hook을 통한 상태 은닉화 (Encapsulation)**
예약/결제 도메인 상태를 각 기능 라우터 내에서만 Provider로 묶고, \`useReservation\` 같은 Custom Hook을 통해서만 해당 상태에 접근하도록 설계했습니다. 이를 통해 **해당 라우터 밖에서는 상태를 변형할 수 없도록 강제하여 부수 효과(Side Effect)를 원천 차단**했습니다.

### **3) 서버 중심 결제 플로우와 보상 트랜잭션**
\`사전 예약 데이터 DB 생성 ➝ PG 승인 요청 ➝ 서버에서 승인 결과 최종 검증 ➝ 확정\`의 단일 흐름을 구축했습니다. 모바일 결제 복귀 시점의 가격/재고 변동으로 파트너사 API 오류가 발생할 경우, **즉각 결제를 취소하고 적절한 에러 메시지와 함께 상품 선택 페이지로 리다이렉트**시키는 정교한 에러 핸들링을 구현했습니다.

### **4) 캐시 라이프사이클(TTL) 통제**
\`localStorage\`에 저장된 입력 데이터에 TTL을 부여하고, 페이지 완전 이탈이나 예약 완료 시 즉시 캐시를 무효화하는 전략을 수립하여 메모리 누수와 잘못된 데이터 복원을 방지했습니다.

## 5. 결과 및 영향

- **유지보수 비용 감소:** 신규 프로젝트(호텔) 온보딩 시 코드를 수정하는 대신 Config 설정만으로 기능 확장이 가능해져 프론트엔드 파편화를 완벽히 방어했습니다.
- **시스템 안정성 확보:** 도메인별 Context 캡슐화와 라우터 레벨 가드(Guard)를 통해 비정상적인 예약 플로우 접근을 100% 차단했습니다.
- **결제 무결성 달성:** 모바일 UX를 보호하는 캐싱 전략과 서버 중심의 깐깐한 결제 검증 로직으로, 운영 기간 중 결제/재고 불일치 오류 0건을 기록했습니다.

## 6. 배운 점 및 확장 사고 (Insight)

- **플랫폼은 '코드의 공유'가 아니라 '책임의 분리'다:** 복제 기반의 시스템이 진정한 플랫폼으로 진화하려면, 비즈니스 분기 로직을 개별 컴포넌트가 아닌 상위 Config 레벨에서 중앙 통제해야 한다는 아키텍처적 깨달음을 얻었습니다.
- **Context API와 캡슐화의 힘:** 도메인 주도 설계(DDD) 관점에서 기능별로 Provider를 쪼개고 Hook으로 접근을 제한하면, 리렌더링 최적화뿐만 아니라 코드의 예측 가능성(Predictability)이 극대화됨을 체감했습니다.
- **차세대 아키텍처 구상 (SSR 및 서버 상태 관리):** 향후 SEO 최적화와 초기 로딩 속도 개선을 위해 Next.js 기반의 SSR 구조를 도입한다면, Config 데이터를 렌더링 전에 주입하는 서버 사이드 구조로 재설계해야 함을 인지하고 있습니다. 또한 클라이언트 상태와 서버 상태를 명확히 분리하기 위해 \`React Query\` 도입을 적극 검토 중입니다.`
  },
  {
    slug: 'integrated-reservation-platform',
    title: '호텔 예약 · 결제 통합 플랫폼',
    description: '1인 백엔드 및 PM/PL로서 요구사항 정의부터 Core 아키텍처 설계, 동시성 제어, 인프라 트러블슈팅(Cloudflare)까지 전 과정을 주도하며, 모놀리식 환경에서 최적의 성능과 데이터 정합성을 이끌어낸 프로젝트입니다.',
    iconName: 'Server',
    category: 'Backend',
    techStack: ['TypeScript', 'NestJS', 'MySQL', 'Prisma', 'Jenkins', 'PM2', 'Cloudflare'],
    status: 'In Progress',
    apiCount: 0,
    overview: '호텔 객실 조회부터 예약, 결제, 관리자 운영 시스템까지 아우르는 통합 예약 플랫폼을 단독으로 기획하고 설계, 개발 중인 프로젝트입니다. 단순한 기능 구현을 넘어 고객사 요구사항을 직접 조율하며, 비즈니스 확장성을 고려한 Core Product 중심의 아키텍처 수립과 완벽한 데이터 정합성 보장에 집중하고 있습니다.',
    period: '2025.12 – 진행 중',
    team: 'FE 3명 / BE 1명 (본인) / PM·PL 겸임',
    content: `## 1. 프로젝트 개요

호텔 객실 조회부터 예약, 결제, 관리자 운영 시스템까지 아우르는 통합 예약 플랫폼을 단독으로 기획하고 설계, 개발 중인 프로젝트입니다. 단순한 기능 구현을 넘어 고객사 요구사항을 직접 조율하며, 비즈니스 확장성을 고려한 Core Product 중심의 아키텍처 수립과 완벽한 데이터 정합성 보장에 집중하고 있습니다.

## 2. 직면한 문제 상황

**1) 파편화된 상품 구조와 기술 부채**
이전 시스템은 상위 Product 개념 없이 테이블이 파편화되어 있었고, 주문 테이블에서 FK 대신 "테이블명+id" 형태의 문자열로 데이터를 참조해 애플리케이션 레벨의 하드코딩과 분기 처리가 불가피했습니다.

**2) 예약·결제·재고의 치명적인 동시성 충돌**
실시간으로 감소해야 하는 재고와 외부 의존성이 높은 결제 로직이 맞물리면서, 다수 사용자의 동시 예약 시도나 외부 결제 API 실패 시 데이터 상태가 불일치하는 리스크가 컸습니다.

**3) 네트워크 레벨의 프론트/서버 쿠키 유실**
도메인이 분리된 배포 환경에서 \`httpOnly\`, \`secure\` 옵션을 적용한 인증 쿠키가 SameSite 정책 등의 문제로 정상적으로 세팅되지 않는 원인 불명의 네트워크 이슈가 발생했습니다.

## 3. 접근 전략

**1) 다형성(Polymorphism)을 적용한 Core Product 모델링**
마이크로서비스(MSA) 대신 내부 모듈화가 잘 된 Monolithic 구조를 택했습니다. 모든 상품의 뿌리를 'Core Product' 테이블로 통합하고 \`type\` 컬럼으로 상품 종류를 식별하게 한 뒤, 하위 테이블과 주문 테이블이 Core의 PK를 FK로 참조하도록 설계하여 분기 로직을 완벽히 제거했습니다.

**2) Optimistic Lock 기반 단일 트랜잭션 통제**
예약 생성, 재고 차감, 결제 검증을 Prisma \`$transaction\`을 활용해 반드시 하나의 흐름으로 묶고, 동시성 제어를 위해 비관적 락 대신 성능에 유리한 낙관적 락(Optimistic Lock) 전략을 채택했습니다.

**3) 라우트 단위 일괄 통제를 위한 Middleware 채택**
관리자(\`/admin\`) 경로의 통합적인 JWT(Access/Refresh/CSRF) 검증 및 재발급을 위해, 개별 컨트롤러에 Guard를 붙이는 대신 \`MiddlewareConsumer\`를 활용하여 특정 경로에 인증 로직을 일괄 적용하는 효율적인 아키텍처를 선택했습니다.

## 4. 구체적 해결 과정

**1) 전략적 비정규화와 JOIN 성능 방어**
완전한 정규화 대신 JOIN Depth를 최대 3단계로 제한했습니다. 최하위 재고 테이블(\`productRoomStocks\`)이 객실 테이블(\`productRooms\`)을 직접 참조하게 하는 전략적 비정규화를 통해 조회 쿼리의 복잡도를 낮추고 성능을 방어했습니다.

**2) 동시성 제어와 UX를 고려한 예외 처리**
\`version\` 컬럼을 활용해 재고 차감을 시도하고, 누군가 먼저 결제하여 충돌(\`PrismaClientKnownRequestError\`)이 발생할 경우 즉각 전체 로직을 Rollback 했습니다. 이때 단순 품절이 아닌 동시성 충돌임을 감안하여, 프론트엔드에 "결제 오류 발생 - 잠시 후 다시 시도해 주세요"라는 유저 친화적 메시지를 전달해 이탈을 방지하고 재결제를 유도했습니다.

**3) Cloudflare 봇 차단 트러블슈팅**
배포 환경의 쿠키 유실 이슈를 추적하던 중, 리버스 프록시 단(Cloudflare)에서 서버 간의 통신을 봇 트래픽으로 오인해 요청을 차단하고 있음을 파악하고 인프라 설정을 우회하여 네트워크 레벨의 장애를 해결했습니다.

**4) MiddlewareConsumer 기반 인증/인가 제어**

\`\`\`tsx
configure(consumer: MiddlewareConsumer) {
  consumer
    .apply(AdminAuthMiddleware)
    .exclude({ path: '*prefix/admin/auth/login', method: RequestMethod.POST }) 
    .forRoutes('*prefix/admin', UploadController);
}
\`\`\`

위와 같이 라우팅 레벨에서 로그인 등 예외 API를 뺀 나머지 관리자 전체 경로에 인증 미들웨어를 일괄 적용하여, 토큰 재발행 및 \`req.user\` 주입 로직을 중앙화했습니다.

## 5. 결과 및 영향

- **정합성 100% 보장:** 동시성 제어와 강력한 트랜잭션 통제를 통해 결제/재고 데이터 불일치 이슈를 완벽하게 차단하고 PM2 Cluster 환경에서도 안정적으로 동작하게 만들었습니다.
- **유지보수성 극대화:** Core Product 중심의 설계로 애플리케이션 레벨의 복잡한 \`if-else\` 분기 로직을 제거하여, 향후 신규 숙박 상품이 추가되더라도 DB 스키마 수정 없이 유연하게 확장 가능한 코어 구조를 완성했습니다.

## 6. 배운 점 및 확장 사고 (Insight)

- **NestJS 라이프사이클: Middleware vs Guard의 고찰:** 프로젝트 초반에는 \`req\` 객체 조작과 쿠키 제어를 위해 Middleware를 선택했습니다. 이후 Guard의 \`ExecutionContext\`를 통해서도 동일한 처리가 가능함을 학습했으나, 이미 세팅된 \`/admin\` 하위의 수많은 API에 \`@UseGuards()\`를 일일이 선언하는 것보다 \`MiddlewareConsumer\`를 통한 라우트 기반 일괄 제어가 현재의 유지보수성에 훨씬 적합하다고 판단하여 아키텍처를 유지했습니다. 이를 통해 프레임워크의 도구를 언제, 어떻게 트레이드오프 해야 하는지 깊이 이해하게 되었습니다.
- **"정규화 vs 성능은 정답이 아닌 균형의 문제다":** 도메인 특성에 따라 완벽한 정규화가 능사가 아니며, 조회 성능 확보를 위해 전략적인 데이터 중복(직접 참조)이 필수적이라는 실무적 감각을 체득했습니다.
- **차세대 아키텍처 구상:** 향후 대규모 트래픽 발생 시, 현재 구조를 바탕으로 객실 조회 결과 캐싱, 재고 집계 테이블 분리, 나아가 읽기와 쓰기를 분리하는 **CQRS 패턴**의 도입과 결제 웹훅 연동을 통한 비동기 처리 고도화를 구상하고 있습니다.`
  }
];
