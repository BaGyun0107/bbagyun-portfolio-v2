// prisma/data/features.ts
// 실제 작업물(Feature) 데이터를 이 파일에서 관리합니다.
// 새 프로젝트 추가 시 아래 배열에 객체를 추가하세요.

// 프로젝트명 (제목)
// 간단한 설명 (1~2문장)
// 기술 스택 (예: NestJS, PostgreSQL, Redis...)
// 개발 기간 (예: 2024.07 ~ 2024.11)
// 역할/팀 구성 (예: 백엔드 단독, 또는 팀 2명)
// 상태 (Production / Beta / Archived)
// 프로젝트 개요 및 본문 (마크다운 가능, 문제 상황/접근 방법/결과 등)

export type SeedFeature = {
  slug: string;
  title: string;
  description: string;
  iconName: string;
  category: string;
  techStack: string[];
  status: string;
  apiCount: number;
  overview: string;
  period?: string | null;
  team?: string | null;
  content?: string | null;
};

export const REAL_FEATURES: SeedFeature[] = [
  {
    slug: 'the-siena-golf-reservation',
    title: '더시에나 골프 예약 시스템 구축',
    description:
      '외부 API 의존도가 높은 예약 시스템에서, 인메모리 버퍼링 기반의 파일 로깅 아키텍처를 도입하여 DB 병목을 해결하고 시스템 안정성을 확보한 프로젝트입니다.',
    iconName: 'CalendarCheck',
    category: 'Fullstack',
    techStack: ['PHP', 'MySQL'],
    status: 'Production',
    apiCount: 0,
    overview:
      '외부 파트너사의 예약 API에 전적으로 의존하는 구조에서, 사용자 예약 흐름과 외부 시스템 간의 안정적인 연계를 목표로 예약/마이페이지 기능 및 통신 로깅 아키텍처를 설계하고 구현했습니다.',
    period: '2023.05 – 2023.06',
    team: '프론트/백 단독',
    content: `## 1. 프로젝트 개요
외부 파트너사의 예약 API에 전적으로 의존하는 구조에서, 사용자 예약 흐름과 외부 시스템 간의 안정적인 연계를 목표로 예약/마이페이지 기능 및 통신 로깅 아키텍처를 설계하고 구현했습니다.

## 2. 문제 상황

### 1) 외부 의존 구조에서의 리스크
예약 데이터가 전적으로 외부 API에 존재하여, 파트너사 서버의 응답 지연이나 타임아웃이 곧바로 사용자 경험 저하(내부 서버 오류로 인식)로 이어지는 구조적 리스크가 있었습니다.

### 2) 비정상 트래픽에 대한 검증 불가
호텔 행사 진행 시 파트너사로부터 "비정상적인 대량 요청이 발생했다"는 클레임이 접수되었으나, 당사 서버에서 발생한 실제 요청 건수나 네트워크 레벨의 재전송 여부를 증명할 객관적 데이터(Trace)가 없었습니다.

### 3) DB 기반 로깅의 병목
초기에는 모든 cURL 통신 로그를 DB에 저장하도록 설계했으나, 수개월 동안 수백만 개의 로그 데이터가 쌓이면서 테이블이 비대해졌고, DB GUI 툴이 다운되는 현상까지 발생했습니다. 메인 비즈니스와 로깅이 동일한 DB 리소스를 공유하며 시스템 안정성을 해치는 역설적인 상황이었습니다.

## 3. 접근 전략

### 1) 외부 장애에 대한 UX 방어 설계
단순 예외 처리를 넘어 외부 API 실패를 사용자 경험 관점에서 재해석했습니다. 타임아웃을 "일시적 혼잡"으로 안내하고, 요청-응답 구간을 시각화하여 사용자의 혼란을 방지하고자 했습니다.

### 2) 로깅 아키텍처 분리 (DB → File)
DB는 핵심 비즈니스 데이터를 위한 자원으로 유지해야 한다고 판단했습니다. 쓰기 위주(Write-heavy)이며 트랜잭션 정합성이 덜 중요한 로그의 특성을 고려해, 비즈니스 DB와 로깅을 분리(Decoupling)하는 전략을 세웠습니다.

### 3) Fail-Safe 설계
"로깅은 실패할 수 있지만, 예약은 실패하면 안 된다"는 대원칙을 세우고, 로그 기록 실패가 메인 비즈니스 로직에 영향을 주지 않도록 모듈을 철저히 격리하기로 했습니다.

## 4. 구체적 해결 과정

### 1) 중복 요청 방어
요청 직후 버튼을 비활성화하고 로딩 상태를 명확히 하여, 클라이언트 단에서 다중 클릭으로 인한 서버 단 중복 요청 가능성을 사전에 차단했습니다.

### 2) 인메모리 버퍼링 기반 cURL 로그 시스템 구축
DB 로깅을 제거하고 날짜 단위(YYYY-MM-DD) 파일 로그 시스템을 직접 구현했습니다. 특히 파일 I/O 병목을 방지하기 위해 **인메모리 버퍼링(Buffering) 패턴을 적용**했습니다. 단건마다 파일에 쓰는 대신, 배열에 로그를 적재하다가 **로그가 20건 쌓이거나 마지막 쓰기 이후 2초가 경과했을 때 일괄적으로 플러시(Flush)** 하도록 구현하여 시스템 부하를 최소화했습니다.

### 3) 로깅 모듈 완전 격리
로깅 모듈 전체를 try-catch로 감싸, 파일 권한 오류나 디스크 용량 부족 문제가 발생하더라도 예약 흐름 자체에는 전혀 영향을 주지 않도록 구현했습니다.

## 5. 결과 및 영향

### 1) 데이터 기반 방어 체계 확보
구축한 파일 로그(요청 시각, 호출 IP, 세션 등)를 바탕으로, 행사 기간 중 발생한 비정상 트래픽이 당사 홈페이지를 통한 정상적인 API 호출 패턴이 아님을 증빙했습니다. 이를 통해 파트너사의 클레임을 객관적 수치로 방어하고 협업 관계를 안정화했습니다.

### 2) 시스템 안정성 개선
로깅 아키텍처 변경 및 버퍼링 도입으로 메인 DB의 병목 현상과 GUI 다운 문제를 완전히 해결하였고, 외부 장애 발생 시에도 유연한 운영 대응이 가능해졌습니다.

## 6. 배운 점 및 확장 사고

### 1) 로그 라이프사이클 관리의 중요성
당시에는 SSH 접속 후 수동 삭제 방식으로 로그를 관리했습니다. 현재 동일 구조를 설계한다면 Logrotate 기반의 자동 파기 정책, 로그 레벨 분리(INFO/WARN/ERROR), 중앙 집중형 로그 수집 아키텍처(ELK, Datadog 등)를 고려해 운영 확장성까지 포함한 설계를 진행했을 것입니다.

### 2) 동시성 제어에 대한 재설계 (현재 관점)
당시에는 "실제 데이터는 외부 시스템에 있으므로 동시성 문제는 파트너사의 책임"이라는 전제를 두었습니다. 현재라면 클라이언트 진입 시점부터 **Redis 기반 TTL Lock (3~5분)** 을 적용할 것입니다. 선점 실패 시 사용자에게 즉시 안내하고 확정 시 Lock을 해제하는 방식으로, 동시성 제어를 외부에 위임하지 않고 우리 시스템 내에서 주도적으로 제어하는 아키텍처를 그릴 것입니다.`,
  },

  {
    slug: 'hanmaum-science-institute',
    title: '한마음과학원',
    description: '77만 자의 비정형 데이터를 파싱하여 DB화하고, MySQL 인덱스와 하이브리드 라우팅을 결합해 대용량 검색 응답 속도를 1500ms에서 400ms로 73% 개선한 프로젝트입니다.',
    iconName: 'Search',
    category: 'Fullstack',
    techStack: ['JavaScript', 'React', 'Node.js', 'Express', 'MySQL', 'Sequelize', 'GitHub Actions', 'PHP'],
    status: 'Production',
    apiCount: 0,
    overview: '77만 자의 비정형 데이터를 파싱하여 DB화하고, MySQL 인덱스와 하이브리드 라우팅을 결합해 대용량 검색 응답 속도를 1500ms에서 400ms로 73% 개선한 프로젝트입니다.',
    period: '2023.06 – 2023.10',
    team: 'FrontEnd 2명 (본인 포함), BackEnd 1명 (본인)',
    content: `## 1. 프로젝트 개요
Word 기반의 교재 데이터를 웹 서비스로 이관하고, 대용량 텍스트 검색 기능과 관리자 업로드 시스템을 구축한 프로젝트입니다. 1인 백엔드 개발자로서 교재 데이터 파싱 아키텍처 설계, RDBMS 기반 검색 최적화, 인증 로직 구현, CI/CD 배포 환경 구성까지 서버사이드 전반을 주도했습니다.

## 2. 문제 상황
### **1) 비정형 대용량 데이터의 DB화 과제**
교재가 오직 Word 파일 형태로만 존재했으며, 이를 구조화하여 DB에 적재해야 했습니다. 최대 454페이지, 약 77만 자에 달하는 텍스트를 파싱하여 Insert 하는 과정에서 인스턴스 서버의 부하 및 데이터 무결성 훼손 리스크가 존재했습니다.
### **2) 검색 성능 및 정확도의 한계**
단락 단위의 텍스트 검색 기능이 필수적이었으나, 기존 검색 방식은 응답 시간이 1500ms에 달해 UX를 크게 저해했습니다. 제한된 일정과 리소스 내에서 Elasticsearch 같은 전문 검색 엔진 도입 없이 빠르고 정확한 검색을 구현해야 했습니다.
### **3) 수동 배포로 인한 운영 리스크**
수동 배포 과정에서 휴먼 에러 발생 가능성이 컸으며, 프로젝트 릴리즈 이후의 운영 안정성을 보장할 파이프라인이 부재했습니다.

## 3. 접근 전략
### **1) 정규식 기반 데이터 파싱 및 로컬 마이그레이션**
Word 파일을 HTML로 변환 후 정규식 기반으로 구조화하는 전략을 세웠습니다. 특히 인스턴스 서버의 스펙 한계로 인한 다운타임을 방지하기 위해, 대용량 파싱 및 DB 적재 프로세스를 로컬 스크립트로 분리하여 실행하는 현실적인 마이그레이션 방안을 채택했습니다.
### **2) 하이브리드 검색 라우팅 (MySQL FULLTEXT + LIKE)**
MySQL InnoDB의 FULLTEXT 기능을 최대한 활용하되, 최소 토큰 길이(\`innodb_ft_min_token_size\`) 제약을 백엔드 애플리케이션 레벨의 로직으로 보완하는 하이브리드 검색 아키텍처를 설계했습니다.
### **3) 운영 및 보안 안정성 확보**
토큰 기반(Access/Refresh) 인증 및 쿠키 보안 설정을 적용하고, GitHub Actions를 활용해 무중단 자동 배포 환경을 구축하여 운영 효율성을 높였습니다.

## 4. 구체적 해결 과정
### **1) 3단계 정규식 패턴을 활용한 대용량 데이터 파싱**
- \`mammoth\` 라이브러리를 활용해 Word 문서를 HTML로 변환했습니다.
- 고객사가 1차 분류한 태그 형태(\`[xx-1-1-1]\`, \`[xx-1-1]\`, \`[xxxx-1]\`)를 분석하여 **3가지 핵심 정규식 패턴**을 도출해 냈고, 이를 기준으로 카테고리, 볼륨(Vol), 단락을 도메인 식별자로 분리했습니다.
- 예외적인 비정형 텍스트(약 2~3천 단어)는 파싱 로직을 과도하게 복잡하게 만드는 대신 클라이언트와 협의하여 수동 처리하는 유연함을 발휘했습니다.
- 파일당 77만 자 분량의 데이터를 로컬 환경에서 건당 3분 이내에 파싱 및 적재 완료하였으며, \`Sequelize\` 트랜잭션을 적용해 실패 시 Rollback 되도록 하여 데이터 무결성을 확보했습니다.
### **2) 백엔드 검색 라우팅 구현 및 정렬 로직 최적화**
- Node.js 백엔드 단에서 검색어 길이를 동적으로 계산하는 분기 로직을 직접 구현했습니다. **3글자 이상은 FULLTEXT BOOLEAN MODE로, 2글자 이하의 단어는 LIKE 검색으로 라우팅**되도록 처리했습니다.
- 정확히 일치하는 단락에 최고 가중치(Relevance)를 부여하고, 인접 단락 간의 비교 로직을 추가하여 문맥 기반의 정렬(스코어링) 알고리즘을 구현했습니다.
### **3) 인증 설계 및 CI/CD 자동화 구축**
- Access / Refresh Token 구조를 채택하고, \`httpOnly\`와 \`secure\` 옵션을 적용해 XSS 공격을 방어했습니다. 토큰에는 최소한의 Payload(ID)만 담고 사용자 정보는 별도 API로 조회하도록 설계했습니다.
- \`master\` 브랜치 Merge 시 GitHub Actions가 트리거되어 SSH 공개키 인증 방식으로 서버에 자동 배포되도록 구성했습니다.

## 5. 결과 및 영향
- **검색 성능 73% 향상 (1500ms → 400ms):** 하이브리드 검색 라우팅 설계 적용 후, 검색되는 데이터의 양과 정확도가 상승했음에도 불구하고 응답 시간은 400ms 수준으로 대폭 단축되었습니다.
- **안정적인 데이터 이관 완수:** 총 5개의 대용량 교재 파일을 데이터 유실이나 서버 장애 없이 성공적으로 서비스 DB에 안착시켰습니다.
- **운영 효율성 증대:** 수동 배포 제거를 통해 백엔드 인프라 운영의 안정성을 크게 높였습니다.

## 6. 배운 점 및 확장 사고 (Insight)
이 프로젝트를 통해 백엔드 설계에 대한 시야를 한 단계 확장할 수 있었습니다.
- **검색 설계에 대한 인사이트:** RDBMS FULLTEXT 기반의 하이브리드 검색으로 성과를 냈으나, 현재 동일한 요구사항을 마주한다면 형태소 분석기(Nori)를 연동한 Elasticsearch 도입을 1순위로 고려할 것입니다. BM25 기반의 스코어링 방식과 검색 로그를 활용한 Relevance 튜닝을 통해 더 전문적인 검색 엔진 아키텍처를 설계해 보고 싶습니다.
- **보안 설계 관점의 성숙:** 당시 쿠키 기반 인증을 구현하며 XSS는 방어했으나 CSRF 취약점에 대한 고려는 부족했습니다. 보안 설계는 단순한 "구현"이 아니라 "공격 벡터를 가정하는 방어적 사고"가 핵심임을 깨달았습니다. 현재라면 SameSite 정책 명확화, Double Submit Cookie 방식 적용, Refresh Token Rotation 도입을 통해 더 견고한 인증 체계를 구축할 것입니다.
- **배포 구조 및 데이터 처리 인프라 고도화:** 인스턴스 서버 부하를 우려해 로컬 마이그레이션을 선택한 것은 현실적인 대응이었으나, 현재라면 Node.js 비동기 워커(Worker) 분리 또는 청크(Chunk) 단위 스트리밍 처리를 통해 서버 자체에서 대용량 데이터를 안전하게 소화하도록 설계할 것입니다. 또한, 단순 직배포 구조를 넘어 Docker 기반의 배포나 롤백 전략이 포함된 CI/CD로 파이프라인을 고도화할 것입니다.`,
  }
];
