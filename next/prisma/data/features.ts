// prisma/data/features.ts
// 실제 작업물(Feature) 데이터를 이 파일에서 관리합니다.
// 새 프로젝트 추가 시 아래 배열에 객체를 추가하세요.

// 프로젝트명 (제목)
// 간단한 설명 (1~2문장)
// 기술 스택 (예: NestJS, PostgreSQL, Redis...)
// 개발 기간 (예: 2024.07 ~ 2024.11)
// 역할/팀 구성 (예: 백엔드 단독, 또는 팀 2명)
// 프로젝트 개요 및 본문 (마크다운 가능, 문제 상황/접근 방법/결과 등)

export type SeedFeature = {
  slug: string;
  title: string;
  description: string;
  iconName: string;
  category: string;
  techStack: string[];
  status: string;
  apiCount: number;
  overview: string;
  period?: string | null;
  team?: string | null;
  content?: string | null;
};

export const REAL_FEATURES: SeedFeature[] = [
  {
    slug: 'the-siena-golf-reservation',
    title: '골프 예약 시스템 구축',
    description:
      '더시에나 골프장의 외부 API 의존도가 높은 예약 시스템에서, 인메모리 버퍼링 기반의 파일 로깅 아키텍처를 도입하여 DB 병목을 해결하고 시스템 안정성을 확보한 프로젝트입니다.',
    iconName: 'CalendarCheck',
    category: 'Fullstack',
    techStack: ['PHP', 'MySQL'],
    status: 'Production',
    apiCount: 0,
    overview:
      '외부 파트너사의 예약 API에 전적으로 의존하는 구조에서, 사용자 예약 흐름과 외부 시스템 간의 안정적인 연계를 목표로 예약/마이페이지 기능 및 통신 로깅 아키텍처를 설계하고 구현했습니다.',
    period: '2023.05 – 2023.06',
    team: 'FE/BE 단독',
    content: `## 1. 프로젝트 개요 및 역할

외부 파트너사의 예약 API를 연동하여 골프 예약 및 마이페이지 기능을 구축하는 프로젝트를 단독으로 수행했습니다. 예약 데이터의 주도권이 전적으로 외부 시스템에 있는 한계 속에서, 안정적인 예약 흐름을 보장하고 외부 장애로부터 내부 시스템과 사용자 경험(UX)을 방어하는 아키텍처를 설계하는 데 집중했습니다.

## 2. 핵심 챌린지 및 문제 해결

### 챌린지 1: 외부 API 100% 의존 환경에서의 예약 연동 및 UX 방어

#### 문제 상황
모든 예약 데이터가 파트너사 서버에 존재했기 때문에, 두 가지 구조적 리스크가 동시에 존재했습니다.

1. **장애 전파**: 파트너사 서버가 3~5초 이상 응답하지 않으면 당사 예약 페이지도 멈추며 사용자에게 "사이트가 고장났다"는 인상을 줌
2. **중복 예약 위험**: 응답 지연 중 사용자가 버튼을 반복 클릭하면, 동일 예약이 파트너사에 여러 번 전송될 우려

#### 고려한 대안과 판단
| 대안 | 접근 | 기각 사유 |
|------|------|-----------|
| 서버 큐잉 | 서버에서 요청을 큐에 넣고 중복 필터링 | PHP 요청당 프로세스 모델에서 서버 사이드 큐 유지 불가, Redis 등 별도 인프라 필요 |
| DB 기반 잠금 | 예약 요청 시 DB에 잠금 레코드 생성 | 외부 API가 데이터 원본이므로 내부 DB 상태와의 동기화 문제 발생 |
| **클라이언트 선제 차단 + UX 장애 격리 (채택)** | 프론트에서 중복 차단, 외부 장애를 사용자에게 우아하게 안내 | — |

추가 인프라 없이 가장 빠르게 적용 가능하며 사용자 경험을 직접 제어할 수 있다는 점에서 채택했습니다.

#### 해결 과정
- **UX 관점의 장애 격리:** 외부 API 실패를 단순 에러(\`500\`)로 처리하지 않고, HTTP 상태 코드와 응답 시간에 따라 분기하는 안내 로직을 설계했습니다. 타임아웃(30초 초과) 시 "일시적 혼잡" 안내, 서버 에러(5xx) 시 "잠시 후 재시도" 안내로, 외부 장애가 당사 서비스 신뢰도 하락으로 이어지지 않도록 했습니다.
- **중복 요청 원천 차단:** 예약 흐름 전 구간에 걸쳐 상태 기반 UI 잠금을 적용했습니다. 요청 전송 즉시 버튼 비활성화 + 스피너 표시, PHP 세션을 활용해 동일 세션 내 2초 이내 재요청을 서버에서도 필터링했습니다.

#### 결과
- 중복 예약 발생 건수 **0건** 달성
- 외부 API 장애 시에도 사용자 이탈 없이 재시도 유도 성공

### 챌린지 2: 수백만 건의 API 통신 로그 병목 해결 및 아키텍처 분리

#### 문제 상황
초기에는 모든 cURL 통신 로그를 메인 비즈니스 DB에 직접 INSERT했습니다. 수개월 만에 **수백만 건**이 적재되면서 두 가지 심각한 문제가 동시에 발생했습니다.

1. **DB 병목**: 예약·회원 데이터와 로그가 동일한 DB를 공유하면서 로그 테이블 비대화로 DB GUI 툴 다운, 예약 쿼리 성능 저하
2. **증빙 불능**: 파트너사로부터 "비정상 대량 요청" 클레임이 접수되었으나, DB 부하로 로깅이 제대로 되지 않아 실제 호출 건수를 증명할 데이터 부족

#### 고려한 대안과 판단
| 대안 | 접근 | 기각 사유 |
|------|------|-----------|
| DB 파티셔닝 | 월별 파티셔닝 또는 아카이빙 | 쓰기 비율 99%인 로그 특성에 부적합, 근본적 DB 부하 미해결 |
| 별도 로그 DB | 로그 전용 DB 구축 | 인프라 비용 증가, 소규모 프로젝트에 과도한 구성 |
| **파일 시스템 + 인메모리 버퍼링 (채택)** | DB 완전 우회, 버퍼링으로 I/O 최소화 | — |

로그는 쓰기가 99% 이상이고 실시간 쿼리가 불필요하므로, 파일 시스템이 가장 적합하며 추가 인프라 없이 즉시 적용 가능하다고 판단했습니다.

#### 해결 과정
- **Fail-Safe 분리 원칙:** "로깅은 실패해도 예약은 절대 실패하면 안 된다"는 원칙을 세우고, 로깅 모듈 전체를 \`try-catch\`로 격리. 파일 권한 오류나 디스크 부족이 발생해도 메인 예약 트랜잭션에 영향 없음
- **인메모리 버퍼링:** 매 요청마다 파일에 쓰는 대신 배열에 적재하고, 20건 또는 2초마다 일괄 플러시. 파일 쓰기 횟수를 약 **95% 감소**
- **구조화된 증빙 체계:** 호출 시각·응답 코드·응답 시간·IP·세션 ID를 구조화된 포맷으로 기록

> 이 챌린지의 기술적 상세(버퍼링 구현, 설계 근거, 코드 레벨 분석)는 인사이트 **[외부 연동 시스템에서의 로깅 분리와 버퍼링 전략](/insights/logging-decoupling-and-buffering-in-external-api-systems)**에서 깊이 있게 다루고 있습니다.

#### 결과
- **DB 병목 완전 해결**: 로그 테이블 제거 후 메인 DB 정상화
- **파트너사 클레임 방어 성공**: 파일 로그 데이터를 근거로 비정상 트래픽 원인이 당사가 아님을 증빙
- **파일 I/O 95% 감소**: 요청당 1회 → 20건당 1회

## 3. 프로젝트 회고

### 지금 다시 만든다면
- **동시성 제어:** 클라이언트 차단만으로는 한계가 있습니다. Redis 기반 TTL Lock(3~5분)을 적용하여, 동일 시간대·동일 자원에 대한 동시 접근을 서버 레벨에서 제어할 것입니다.
- **로그 라이프사이클:** 이후 Node.js 프로젝트에서 \`winston\` + \`winston-daily-rotate-file\`을 경험하며, 로그 레벨 분리와 자동 파기 정책의 필요성을 체득했습니다. ELK 스택 기반 중앙 집중형 수집 체계로 확장하여 운영 가시성을 확보할 것입니다.`,
  },
  {
    slug: 'hanmaum-science-institute',
    title: '법문검색 엔진 구축',
    description: '한마음과학원 사이트의 77만 자의 비정형 데이터를 파싱하여 DB화하고, 레거시 DB 환경의 제약을 극복한 FTS + 동적 LIKE 하이브리드 검색 엔진을 고안해 응답 속도를 1500ms에서 400ms로 73% 개선한 프로젝트입니다.',
    iconName: 'Search',
    category: 'Fullstack',
    techStack: ['JavaScript', 'React', 'Node.js', 'Express', 'MySQL', 'Sequelize', 'GitHub Actions', 'PHP'],
    status: 'Production',
    apiCount: 0,
    overview: '77만 자의 비정형 데이터를 파싱하여 DB화하고, 레거시 DB 환경의 제약을 극복한 FTS + 동적 LIKE 하이브리드 검색 엔진을 고안해 응답 속도를 1500ms에서 400ms로 73% 개선한 프로젝트입니다.',
    period: '2023.06 – 2023.10',
    team: 'FE 2명 (본인 포함), BE 1명 (본인)',
    content: `## 1. 프로젝트 개요 및 역할

Word 기반의 교재 데이터(최대 454페이지, 약 77만 자)를 웹 서비스로 이관하고, 단락 단위 검색 기능과 관리자 업로드 시스템을 구축한 프로젝트입니다. 1인 백엔드 개발자로서 데이터 파싱 설계, 검색 엔진 최적화, 인증 구현, GitHub Actions 기반 CI/CD 구축까지 서버사이드 전반을 주도했습니다.

## 2. 핵심 챌린지 및 문제 해결

### 챌린지 1: 레거시 DB 제약을 극복한 하이브리드 검색 엔진 구축 (1500ms → 400ms)

#### 문제 상황
단락 단위의 대용량 텍스트 검색이 핵심 기능이었으나, 세 가지 제약이 동시에 존재했습니다.

1. **성능**: 기존 \`LIKE '%검색어%'\` 쿼리가 Full Table Scan을 수행하여 평균 **1500ms** 소요
2. **DB 버전 제약**: MySQL 버전이 낮아 N-gram 파서 사용 불가, \`ft_min_word_len\`이 2로 강제
3. **한국어 특성**: 1글자 검색과 조사 처리가 FULLTEXT 인덱스만으로는 불가능

#### 고려한 대안과 판단
| 대안 | 접근 | 기각 사유 |
|------|------|-----------|
| Elasticsearch 도입 | 전문 검색 엔진으로 전환 | 프로젝트 일정·인프라 예산 제약으로 외부 인프라 추가 불가 |
| LIKE 쿼리 최적화 | 인덱스 힌트, 쿼리 튜닝 | Full Table Scan 구조 자체를 바꾸지 못하므로 근본적 한계 |
| **FTS + 동적 LIKE 하이브리드 (채택)** | FULLTEXT로 후보군 압축 후 LIKE로 정밀 검증 | — |

외부 인프라 없이 현재 RDBMS 환경을 극한까지 활용하는 것이 현실적인 판단이었습니다. "속도는 FTS가, 정확도는 LIKE가 담당"하는 역할 분리를 핵심 설계 원칙으로 세웠습니다.

#### 해결 과정
- **1단계 — FTS로 후보군 압축**: 검색어에서 2글자 이상 단어를 추출하여 \`FULLTEXT BOOLEAN MODE\` 쿼리를 실행. 동적 연산자(\`+단어*\`)를 붙여 조사 대응력을 높이고, 수십만 건을 수백 건으로 압축
- **2단계 — LIKE로 정밀 검증**: 압축된 후보군 대상으로, 1글자 포함 모든 검색어가 실제 문장에 존재하는지 \`LIKE\`로 최종 필터링
- **Node.js 라우팅 분기**: 검색어 길이에 따라 FTS/LIKE 경로를 동적으로 분기하는 하이브리드 라우팅을 애플리케이션 레이어에서 구현

> 이 챌린지의 기술적 상세(역인덱스 원리, 하이브리드 라우팅 코드, 성능 측정 과정)는 인사이트 **[1500ms에서 400ms로: RDBMS 환경에서 77만 자 대용량 텍스트 검색 최적화](/insights/optimizing-770k-text-search-in-rdbms)**에서 깊이 있게 다루고 있습니다.

#### 결과
- 검색 응답 **1500ms → 400ms (73% 개선)**
- 1글자 및 한국어 조사 검색 정확도 확보
- 데이터 증가에도 응답 시간 안정적 유지 (인덱스 기반)

### 챌린지 2: 77만 자 비정형 데이터 파싱 및 실용적 마이그레이션

#### 문제 상황
최대 454페이지, 약 77만 자에 달하는 Word 교재를 구조화하여 DB에 적재해야 했습니다. 두 가지 제약이 있었습니다.

1. **비정형 데이터**: Word 파일 내부에 일관된 구조가 없고, 카테고리/볼륨/단락 구분이 서식에 의존
2. **서버 스펙 한계**: 인스턴스에서 대용량 파싱을 직접 수행하면 다운타임 및 데이터 무결성 훼손 리스크

#### 고려한 대안과 판단
| 대안 | 접근 | 기각 사유 |
|------|------|-----------|
| 서버 내 비동기 파이프라인 | 서버에서 업로드 → 파싱 → 적재 자동화 | 연 1~2회 갱신에 상시 파이프라인은 오버엔지니어링 |
| 수동 CSV 변환 | Word → Excel → CSV → DB 임포트 | 77만 자 서식 정보 손실, 반복 작업 시 휴먼 에러 |
| **로컬 스크립트 + 트랜잭션 (채택)** | 로컬에서 파싱 후 트랜잭션으로 일괄 적재 | — |

연 1~2회 발생하는 갱신 작업에 서버 내 파이프라인을 구축하는 것은 과도한 투자라고 판단했습니다. 로컬 스크립트로 분리하되, 트랜잭션으로 데이터 무결성을 보장하는 실용적 접근을 선택했습니다.

#### 해결 과정
- **Word → HTML 변환**: \`mammoth\` 라이브러리로 Word 파일을 HTML로 변환하여 서식 정보 보존
- **3단계 정규식 패턴 도출**: 고객사의 분류 체계를 분석하여 카테고리 → 볼륨 → 단락 식별자를 정규식으로 추출하는 파싱 로직 구현
- **Sequelize 트랜잭션 적용**: 전체 적재를 하나의 트랜잭션으로 묶어 중간 실패 시 롤백. 77만 자 전량 적재 성공 또는 전량 롤백을 보장

#### 결과
- 77만 자 전량 DB 구조화 완료, 데이터 무결성 **100% 보장**
- 파싱 스크립트 재사용으로 이후 교재 업데이트 시에도 동일 프로세스 적용 가능

## 3. 프로젝트 회고

- **검색 엔진 고도화**: RDBMS 하이브리드 라우팅으로 현실적인 타협점을 찾았지만, 토크나이징 세밀도와 결과 랭킹에서 한계가 있었습니다. 대규모 검색 시스템이라면 Elasticsearch를 도입하여 N-gram 설정과 스코어링을 직접 제어할 것입니다.
- **보안 설계 성숙**: 이 프로젝트에서 JWT 인증을 처음 구현하며 \`httpOnly\`/\`secure\` 쿠키로 XSS는 방어했으나, CSRF 대응이 부족했습니다. 이후 프로젝트에서 SameSite 정책과 Double Submit Cookie 방식을 적용하며 보안 설계의 폭을 넓혔습니다.`,
  },
  {
    slug: 'blackstone-belleforet-resort',
    title: '리조트 웹사이트 리뉴얼 및 예약 시스템 구축',
    description: '블랙스톤 벨포레리조트의 레거시(PHP) 환경에 SPA(React)를 결합한 점진적 공존 아키텍처를 설계하고, 결제 보상 트랜잭션과 BFF 기반의 프록시를 도입하여 시스템의 신뢰도와 보안을 크게 끌어올린 프로젝트입니다.',
    iconName: 'Laptop',
    category: 'Fullstack',
    techStack: ['JavaScript', 'React', 'Node.js', 'Express', 'MySQL', 'Sequelize', 'Jenkins', 'PHP'],
    status: 'Production',
    apiCount: 0,
    overview: '레거시(PHP) 환경에 SPA(React)를 결합한 점진적 공존 아키텍처를 설계하고, 결제 보상 트랜잭션과 BFF 기반의 프록시를 도입하여 시스템의 신뢰도와 보안을 크게 끌어올린 프로젝트입니다.',
    period: '2023.12 – 2024.10',
    team: 'FE 3명 (본인 포함) / BE 1명 (본인)',
    content: `## 1. 프로젝트 개요 및 역할

기존 PHP 중심으로 운영되던 리조트 웹사이트에 React를 도입하여, 예약/로그인/마이페이지 등 핵심 구간의 UX를 개선한 프로젝트입니다. 1인 백엔드 겸 프론트엔드 개발자로서 기존 기술 스택을 전면 부정하지 않고, PHP 레거시와 React/Node.js가 충돌 없이 공존하는 아키텍처를 설계했습니다. 파트너사 API 연동, 결제 시스템 통합, 앱 WebView 세션 문제 해결까지 프로젝트 전반을 주도했습니다.

## 2. 핵심 챌린지 및 문제 해결

### 챌린지 1: 레거시(PHP)와 모던 SPA(React)의 라우팅 공존 및 세션 유지

#### 문제 상황
세 가지 이질적인 문제가 동시에 존재했습니다.

1. **라우팅 충돌**: 기존 인프라가 PHP 라우팅에 맞춰져 있어, React(클라이언트 라우팅) 도입 시 서버 라우팅과 충돌하여 404 에러 발생
2. **인증 체계 불일치**: 레거시 PHP는 서버 세션 기반 인증, React SPA는 JWT 토큰 기반 인증으로 양쪽의 로그인 상태를 공유할 수 없는 구조
3. **WebView 세션 유실**: PC/모바일 웹에서는 정상 유지되는 JWT 쿠키 세션이, 앱(WebView) 환경에서만 반복 유실

#### 고려한 대안과 판단
| 대안 | 접근 | 기각 사유 |
|------|------|-----------|
| 서브도메인 분리 | \`app.example.com\`에 React 배포 | CORS 이슈, 쿠키 공유 복잡, 기존 SEO/링크 깨짐 |
| PHP 전면 교체 | React로 전체 재개발 | 운영 중 서비스라 전면 교체 리스크 과대, 일정 부족 |
| 세션/JWT 이중 운영 | PHP는 세션, React는 JWT로 각각 유지 | 로그인 상태 동기화 불가, 사용자가 페이지 이동 시 재로그인 필요 |
| **Nginx 경로 분기 + JWT 통일 (채택)** | \`/app/*\`만 React로 분기, 인증을 JWT로 통일 | — |

Same-Origin을 유지하면서 점진적으로 React를 확장할 수 있고, 인증 체계를 하나로 통일하여 PHP↔React 간 로그인 상태를 자연스럽게 공유할 수 있는 구조가 필요했습니다.

#### 해결 과정
- **Nginx 경로 분기**: \`/app/*\` 요청만 React 빌드 파일로 분기, 직접 URL 접근 시 \`index.html\`로 Fallback. Same-Origin 내에서 PHP와 React가 충돌 없이 공존하는 점진적 아키텍처 구축
- **인증 체계 JWT 통일**: 기존 PHP 세션 기반 로그인을 JWT 발급 방식으로 전환하여, PHP 페이지와 React 페이지 모두 동일한 JWT 쿠키를 참조하도록 통일. PHP 측에서는 JWT를 검증하는 미들웨어를 추가하고, React 측에서는 쿠키의 JWT를 읽어 인증 상태를 판단하도록 구현하여, 페이지 간 이동 시 재로그인 없는 일관된 인증 경험을 제공
- **WebView 세션 해결**: 앱/웹 브라우저의 스토리지·쿠키 정책 차이를 분석한 후, 앱 개발자와 협업하여 브릿지 인터페이스 구축. \`window.postMessage\` 방식으로 토큰을 네이티브 앱에 전달

#### 결과
- PHP 서비스 **무중단** 상태에서 React 도입 완료
- PHP↔React 간 **로그인 상태 완전 공유** (재로그인 0회)
- WebView 환경 세션 유실 **0건** 달성

### 챌린지 2: 결제 파편화로 인한 데이터 불일치 해결

#### 문제 상황
기존 시스템은 "결제 승인 → DB 저장 → 외부 API 호출" 로직이 물리적으로 파편화되어 있었습니다. 중간 단계에서 실패하면 **DB에 예약은 생성되었으나 실제 결제가 누락**되는 데이터 불일치가 간헐적으로 발생했습니다.

#### 고려한 대안과 판단
| 대안 | 접근 | 기각 사유 |
|------|------|-----------|
| 2PC (분산 트랜잭션) | 결제-DB-외부API를 하나의 분산 트랜잭션으로 | PHP 환경에서 구현 과도, 외부 API가 XA 미지원 |
| 이벤트 소싱 | 결제 이벤트를 기록하고 비동기 처리 | 인프라 복잡도 과대, 실시간 결제 확인 필요한 서비스 특성과 부적합 |
| **보상 트랜잭션 (채택)** | 단일 흐름으로 묶고, 실패 시 역방향 취소 | — |

결제는 사용자가 즉시 결과를 확인해야 하므로 동기식 흐름이 적합했습니다.

#### 해결 과정
- **결제 단일 흐름화**: 파편화된 로직을 하나의 흐름으로 통합, 각 단계의 성공/실패를 순차 검증
- **보상 트랜잭션**: 외부 API 실패 시 \`tid\` 존재 여부를 확인하여 즉시 PG사 결제 취소 API 호출. "결제는 됐는데 예약은 안 됨" 상황을 원천 차단
- **UX 방어**: 결제 실패 시 사용자 입력 데이터를 스토리지에서 복원하여 재입력 불편 제거

#### 결과
- 결제-예약 데이터 불일치 **0건** 달성
- 결제 실패 후 재시도 이탈률 감소

### 챌린지 3: 클라이언트 API Key 노출 보안 취약점 대응

#### 문제 상황
초기 React 도입 시 클라이언트에서 외부 파트너사 API를 직접 호출하도록 구현했으나, 브라우저 개발자 도구 네트워크 탭에 **API Key가 그대로 노출**되는 치명적 보안 이슈를 인지했습니다.

#### 고려한 대안과 판단
| 대안 | 접근 | 기각 사유 |
|------|------|-----------|
| 환경변수 난독화 | 빌드 시 Key를 번들에 포함하되 난독화 | 번들 분석으로 추출 가능, 근본 해결 아님 |
| **백엔드 프록시 (채택)** | 모든 외부 API 호출을 서버 경유로 전환 | — |

클라이언트에 시크릿을 두는 것 자체가 근본적으로 잘못된 구조입니다.

#### 해결 과정
- PHP 기반 프록시 서버를 즉시 구축하여 모든 외부 API 통신을 백엔드로 우회. API Key를 서버 환경변수로 이관하여 클라이언트 노출 원천 차단

> 이 챌린지에서 얻은 교훈과 BFF 아키텍처로의 발전 과정은 인사이트 **[API Key 노출 이슈와 BFF 아키텍처로의 발전](/insights/spa-api-key-exposure-and-bff-architecture)**에서 깊이 있게 다루고 있습니다.

#### 결과
- API Key 클라이언트 노출 **즉시 차단**
- 이후 모든 외부 API 호출을 서버 경유 구조로 표준화

## 3. 프로젝트 회고

- **결제 상태 머신**: 결제가 단방향 로직이 아니라 여러 전이 단계를 갖는 프로세스임을 체감했습니다. 이후 프로젝트에서는 \`INIT → APPROVED → CONFIRMED → FAILED\` 등 명확한 상태 머신을 기반으로 결제 파이프라인을 설계하게 되었습니다.
- **API 계약 주도**: 파트너사에도 전례 없던 신규 티켓 기능을 개발하며, API 명세를 기다리는 구현자가 아닌 Response 구조를 먼저 제안하는 역할로 전환했습니다. API를 "시스템 간의 계약"으로 인식하고 기획 단계부터 구조를 리드하는 관점을 가지게 되었습니다.`
  },
  {
    slug: 'integrated-sso-server',
    title: '통합 회원 관리 인증 서버 (SSO) 설계 및 구축',
    description: '사내 서비스들의 파편화된 인증 체계를 통합하기 위해 NestJS 기반의 중앙 집중형 SSO 서버를 구축하고, JWT Payload와 Soft FK를 활용해 물리적으로 분리된 DB 환경에서 안전한 신뢰 모델을 설계한 프로젝트입니다.',
    iconName: 'ShieldCheck',
    category: 'Backend',
    techStack: ['TypeScript', 'NestJS', 'MySQL', 'Prisma', 'Jenkins', 'PM2'],
    status: 'Production',
    apiCount: 0,
    overview: '사내 서비스들의 파편화된 인증 체계를 통합하기 위해 NestJS 기반의 중앙 집중형 SSO 서버를 구축하고, JWT Payload와 Soft FK를 활용해 물리적으로 분리된 DB 환경에서 안전한 신뢰 모델을 설계한 프로젝트입니다.',
    period: '2024.07 – 2024.12',
    team: 'BE 1명 (본인)',
    content: `## 1. 프로젝트 개요 및 역할

사내 여러 서비스에 파편화되어 있던 인증 체계를 하나로 통합하기 위해 TypeScript와 NestJS 기반의 중앙 집중형 통합 회원 관리(SSO) 서버를 단독으로 설계하고 구축했습니다. 단순한 로그인 API 집합이 아닌, 다수의 사내 서비스 간 신뢰 모델을 보장하고 확장이 용이한 Provider 기반 인증 플랫폼을 만드는 것을 목표로 서버사이드 전반을 주도했습니다.

## 2. 핵심 챌린지 및 문제 해결

### 챌린지 1: 물리적으로 분리된 DB 환경에서의 유저 식별 설계

#### 문제 상황

1. 유저 인증 정보는 통합 SSO 서버가 관리해야 하고, 주문·예약 등 비즈니스 데이터는 개별 서비스 DB에 남아야 했습니다.
2. 물리적으로 다른 DB를 사용하므로 RDBMS의 Foreign Key를 직접 연결할 수 없었습니다.
3. 데이터 정합성 유지와 장애 발생 시 원인 추적이 어려워지는 문제가 예상되었습니다.

#### 고려한 대안과 판단

| 대안 | 접근 | 기각 사유 |
|------|------|-----------|
| 단일 통합 DB | 모든 서비스 데이터를 SSO DB에 통합 | 서비스 간 강결합 발생, 장애 전파 범위 확대 |
| API 조회 방식 | 개별 서비스가 매 요청마다 SSO API로 유저 조회 | SSO 서버 부하 집중, 응답 지연 증가 |
| **JWT Payload + Soft FK** | UUID를 토큰에 담아 전달, 각 서비스 DB에 논리적 FK로 저장 | — |

UUID를 Access Token Payload에 포함시키면, 개별 서비스는 토큰 검증만으로 유저를 식별할 수 있어 SSO 서버와의 추가 통신 없이 독립성과 정합성을 동시에 확보할 수 있었습니다.

#### 해결 과정

- 통합 SSO 서버가 로그인 시 유저에게 UUID를 발급하고 Access Token Payload에 포함하여 전달하도록 설계했습니다.
- 개별 서비스는 토큰에서 UUID를 추출해 자신의 DB에 Soft FK(논리적 외래키)로 저장하여, SSO 서버와의 물리적 DB 결합 없이 유저를 완전히 매핑할 수 있도록 구성했습니다.
- Soft FK 방식을 선택함으로써 SSO 서버 장애가 개별 서비스의 비즈니스 데이터에 영향을 주지 않는 격리 구조를 확보했습니다.

> 이 챌린지의 기술적 상세는 인사이트 **[분산 환경에서의 SSO 통합과 Soft FK 활용기](/insights/sso-authentication-and-soft-fk)**에서 깊이 있게 다루고 있습니다.

#### 결과

- DB 간 직접 연결 없이 유저 식별 성공률 100% 달성 — 서비스별 독립 배포 및 장애 격리 가능
- SSO 서버 없이도 개별 서비스가 토큰 자체 검증으로 응답 가능하여 SSO 의존성 제거

---

### 챌린지 2: 파편화된 인증 체계 통합 및 중앙 세션 통제력 확보

#### 문제 상황

1. 서비스마다 JWT Secret Key 관리 방식이 달라 보안 수준이 불균일했습니다.
2. Access Token 만료 정책이 서비스별로 상이해 세션 수명 예측이 불가능했습니다.
3. 보안 사고 발생 시 특정 유저나 디바이스의 세션을 즉각 차단할 수단이 없었습니다.

#### 고려한 대안과 판단

| 대안 | 접근 | 기각 사유 |
|------|------|-----------|
| 각 서비스 자체 토큰 정책 유지 | 서비스마다 독립 관리 | 보안 수준 불균일, 중앙 통제 불가 |
| Redis 기반 세션 관리 | 토큰 대신 세션 ID를 Redis에 저장 | 인프라 추가 비용, 현 규모 대비 오버엔지니어링 |
| **토큰 이중 구조 + RDBMS 저장** | Access Token은 서비스 자체 검증, Refresh Token은 SSO DB 중앙 관리 | — |

Access Token(10분) 자체 검증으로 SSO 부하를 분산하고, Refresh Token(7일)은 RDBMS에 저장하여 강제 로그아웃 시 레코드 삭제만으로 즉각 통제하는 구조가 현 인프라 조건에서 가장 실용적인 판단이었습니다.

#### 해결 과정

- 서비스별 JWT Secret 분리, Authorization Key 발급, 허용 IP 화이트리스트 관리를 도입하여 서비스 간 인증 격벽을 설계했습니다.
- Refresh Token을 Prisma를 통해 RDBMS에 저장하고, 보안 이슈 발생 시 해당 레코드 삭제만으로 특정 디바이스나 유저를 강제 로그아웃하는 메커니즘을 구현했습니다.
- NestJS의 @UseGuards와 Custom Decorator를 활용하여 토큰 검증·Provider 권한 인가 로직을 비즈니스 레이어와 완전히 분리하고, 인증 파이프라인을 선언적으로 구성했습니다.
- 보안 정책 변경 시 Guard 단 한 곳만 수정하면 전체 서비스에 일괄 적용되는 구조로, 향후 정책 변경 비용을 최소화했습니다.

#### 결과

- 사내 전체 서비스 토큰 만료 정책 통일 — 보안 감사 기준 충족
- 강제 로그아웃 응답: 기존 수단 없음 → DB 레코드 삭제 후 다음 Refresh 요청 시 즉각 인증 거부

## 3. 프로젝트 회고

- 인증 설계에서 "토큰을 어떻게 만드느냐"보다 "세션을 어떻게 통제하느냐"가 더 중요한 판단임을 체감했습니다. Refresh Token을 DB에 저장하는 결정 하나가 강제 로그아웃, 디바이스 관리, 감사 추적 등 후속 요구사항 전체를 가능하게 했습니다.
- 현 구조에서 Refresh Token 검증 시 발생하는 DB I/O는 트래픽이 증가할 경우 병목이 될 수 있습니다. Redis 캐싱 레이어 도입과 RTR(Refresh Token Rotation) 전략을 우선 적용해 보안과 성능을 함께 고도화하고 싶습니다.`
  },
  {
    slug: 'hipass-b2b-platform',
    title: '화훼 도소매 B2B 주문 플랫폼',
    description: '1인 백엔드로서 결제/정산 자동화 파이프라인(간이 Outbox 패턴)을 단독 설계하여 금융 데이터 무결성을 100% 확보하고, 프론트엔드의 기능 단위 아키텍처 표준화까지 주도해 시스템 전체의 안정성과 유지보수성을 끌어올린 프로젝트입니다.',
    iconName: 'Store',
    category: 'Fullstack',
    techStack: ['JavaScript', 'React', 'Node.js', 'Express', 'MySQL', 'Sequelize', 'Jenkins', 'Socket.io'],
    status: 'Production',
    apiCount: 0,
    overview: '1인 백엔드로서 결제/정산 자동화 파이프라인(간이 Outbox 패턴)을 단독 설계하여 금융 데이터 무결성을 100% 확보하고, 프론트엔드의 기능 단위 아키텍처 표준화까지 주도해 시스템 전체의 안정성과 유지보수성을 끌어올린 프로젝트입니다.',
    period: '2024.08 – 2025.08',
    team: 'FE 2명 (본인 포함) / BE 1명 (본인)',
    content: `## 1. 프로젝트 개요 및 역할

도매상과 화원 간의 실시간 화훼 거래를 지원하는 B2B 플랫폼입니다. 1인 백엔드 개발자로서 실시간 주문 상태 동기화, 구독 기반 자동 빌링(Toss 연동), 정산 파이프라인 구축 등 코어 서버 로직 전반을 단독 설계했습니다. 프론트엔드(React) 2인 팀의 초기 아키텍처 표준화도 주도하여 팀 전체의 개발 생산성과 코드 일관성을 끌어올렸습니다.

## 2. 핵심 챌린지 및 문제 해결

### 챌린지 1: 비가역적 정산 데이터의 유실 방지 및 무결성 확보

#### 문제 상황
외부 지급대행 API를 통한 정산은 한 번 실행되면 되돌릴 수 없습니다. 공휴일이나 네트워크 장애 시 중복 정산되거나 누락되는 사고를 방어해야 했고, 정산 대상 데이터를 매 실행마다 DB에서 재조회할 때 발생하는 I/O 부하 문제도 동시에 해결해야 했습니다.

#### 고려한 대안과 판단

| 대안 | 접근 | 기각 사유 |
|------|------|-----------|
| DB 상태값만으로 재시도 관리 | 정산 실패 시 상태를 '실패'로 마킹, 다음 실행 시 재조회 | 실패 후 재조회 쿼리 부하 + 이중 실행 위험 잔존 |
| 정규 Outbox 테이블 + MQ | DB 트랜잭션과 메시지 발행을 분리, 메시지 큐로 재시도 | 리소스 제약 환경에서 RabbitMQ/Kafka 도입은 과도한 운영 복잡도 |
| **JSON 파일 기반 간이 Outbox 패턴** | 정산 대상 데이터를 물리 JSON 파일로 저장, 성공 시 삭제 | — |

DB 상태 선점으로 중복 실행을 막고, JSON 파일을 Fallback으로 두어 실패 시 다음 영업일에 파일을 그대로 재사용하는 구조가 현재 팀 규모와 운영 환경에 가장 실용적인 타협점이었습니다.

#### 해결 과정
- 스케줄러(node-schedule) 실행 시 정산 대상을 DB에서 조회한 뒤 상태를 '정산 대기'로 선점하여 중복 실행을 원천 차단했습니다.
- 조회 결과를 물리 JSON 파일로 저장해 이후 API 호출은 DB가 아닌 파일을 참조하도록 구성했습니다.
- 지급대행 API 호출 성공 시에만 파일을 삭제하고, 실패(네트워크 오류, 공휴일 등) 시 파일을 보존해 다음 영업일 재시도의 근거로 활용했습니다.
- 결제 스케줄러는 PM2 단일 프로세스로 격리하고, 주문 생성 → 결제 요청 → 승인 → 실패 시 취소 흐름을 서버 주도 단일 트랜잭션으로 묶어 클라이언트 콜백 의존성을 제거했습니다.

> 이 챌린지의 설계 결정과 기술적 상세는 인사이트 **[JSON 파일을 활용한 간이 Outbox 패턴으로 정산 데이터 지키기](/insights/json-outbox-pattern-for-settlement)**에서 다루고 있습니다.

#### 결과
- 운영 기간 내 결제/정산 데이터 불일치 0건: 서버 통제형 트랜잭션과 JSON Fallback이 금융 데이터 무결성을 완전히 보장했습니다.
- 정산 재실행 시 DB 재조회 횟수를 0회로 줄였습니다. 실패 건은 기존 JSON 파일을 그대로 재사용합니다.
- 공휴일 정산 실패 건 전량 다음 영업일 자동 처리: 수동 개입 없이 파이프라인이 안정적으로 복구됩니다.

---

### 챌린지 2: 트래픽 낭비를 줄이는 실시간 상태 동기화망 구축

#### 문제 상황
도매상과 화원이 '주문 접수 ➝ 승인 ➝ 진행 ➝ 완료' 상태를 즉각적으로 공유해야 했으나, 기존의 HTTP Polling 방식은 트래픽 낭비와 서버 부하를 유발해 B2B 거래의 실시간성을 보장하기에 비효율적이었습니다.
#### 해결 과정 (WebSocket 분기)
비효율적인 Polling을 걷어내고, Socket.io의 Room(User UUID 기반) 시스템을 도입했습니다. 상태 변경 시점에만 관련 도매상과 타겟 화원에게 이벤트를 즉각적으로 Emit 하는 이벤트 파이프라인을 구축하여 서버 부하를 최소화했습니다.

### 챌린지 3: 프론트엔드 아키텍처 파편화 극복 및 표준화 리드

#### 문제 상황
과거 프로젝트에서 정형화된 아키텍처의 부재로 작업자마다 코드 스타일과 디렉터리 구조가 달라져 유지보수에 큰 어려움을 겪었던 한계가 있었습니다.
#### 해결 과정 (기능 중심 모듈화)
과거의 산발적인 구조를 탈피하고자, 전역 상태를 '유저 정보' 수준으로 최소화했습니다. 대신 기능(Feature) 단위로 폴더링을 모듈화하는 디렉터리 아키텍처 규약을 선제적으로 제시하여 철저히 캡슐화된 상태 관리를 리드했습니다.

## 3. 결과 및 회고 (Insight)

- **결제/정산 데이터 불일치 0건:**
서버 통제형 트랜잭션과 JSON Fallback 시스템을 통해 운영 기간 내내 단 한 건의 금융 데이터 오류도 발생하지 않는 높은 신뢰도를 달성했습니다.
- **조직 내 개발 표준 확립:**
주도적으로 설계한 기능 단위 아키텍처가 팀 내 FE 표준으로 자리 잡아 UI/UX 개발 속도와 코드 가독성이 비약적으로 상승했습니다.
- **차세대 아키텍처 고도화 구상**
    - **결제 멱등성 및 비즈니스 확장:** 네트워크 지연에 따른 이중 결제 시도를 원천 차단하기 위해 **Idempotency Key(멱등성 키)**와 DB Unique 제약조건을 활용한 방어 로직의 필요성을 체감했습니다. 잔액 부족 시 즉시 해지가 아닌 Grace Period(유예 기간)를 두는 로직 확장도 구상 중입니다.
    - **정규 Outbox와 MQ 도입:** 현재의 JSON Fallback은 훌륭한 타협점이었으나, 향후 트래픽 증가 시 이를 **DB 기반의 정규 Outbox 테이블**로 승격시키고, 메인 트랜잭션과 이벤트 발행을 분리하는 Message Queue(RabbitMQ, Kafka) 아키텍처로 고도화하여 결합도를 낮추고 싶습니다.
    - **실시간 통신 신뢰성 극대화:** Socket.io Emit 구조에 클라이언트 수신을 보장하는 ACK(Acknowledgement) 구현과 서버 재시작 시의 메시지 복구 전략을 수립하여 통신 신뢰성을 극대화하고자 합니다.`
  },
  {
    slug: 'hotel-reservation-platform',
    title: '호텔 예약 시스템 플랫폼화 및 구조 고도화',
    description: '하드코딩 기반의 복제형 예약 시스템을 "설정 주도(Config-driven) 아키텍처"로 전면 재설계하여, 소스 코드 수정 없이 다수의 신규 프로젝트 확장이 가능한 프론트엔드 플랫폼 환경을 구축한 프로젝트입니다.',
    iconName: 'LayoutTemplate',
    category: 'Fe',
    techStack: ['JavaScript', 'React', 'PHP', 'MySQL', 'Sequelize', 'Jenkins'],
    status: 'Production',
    apiCount: 0,
    overview: '하드코딩 기반의 복제형 예약 시스템을 "설정 주도(Config-driven) 아키텍처"로 전면 재설계하여, 소스 코드 수정 없이 다수의 신규 프로젝트 확장이 가능한 프론트엔드 플랫폼 환경을 구축한 프로젝트입니다.',
    period: '2024.11 – 2025.10',
    team: 'FE 2명 (본인 포함) / BE 1명',
    content: `## 1. 프로젝트 개요 및 역할

단일 호텔에 종속되어 있던 기존 예약 페이지를 다수의 파트너사(호텔)에 유연하게 적용할 수 있는 플랫폼 아키텍처로 전환했습니다. 프론트엔드 2인 팀 내에서 설계 주도 역할을 맡아, 코드 수정 없이 신규 프로젝트를 온보딩할 수 있는 Config-driven 구조 도입, 복잡한 예약 상태 관리 캡슐화, 크로스 플랫폼 결제 정합성 보장까지 핵심 아키텍처 결정을 담당했습니다.

## 2. 핵심 챌린지 및 문제 해결

### 챌린지 1: 하드코딩된 복제 구조 탈피와 Config-driven 플랫폼 설계

#### 문제 상황
결제 방식, 복합 예약(호텔+골프), 다국어, 회원 정책 등 파트너사마다 다른 요구사항을 공통 코드 내 if-else 분기로 처리했습니다. 신규 호텔 온보딩 시마다 공통 컴포넌트를 직접 수정해야 했고, A 호텔 수정이 B 호텔 UI에 사이드 이펙트를 유발하는 상황이 반복되었습니다. 공통 버그가 발생하면 복제된 N개 프로젝트를 모두 수정·배포해야 했습니다.

#### 고려한 대안과 판단

| 대안 | 접근 | 기각 사유 |
|------|------|-----------|
| 복제 코드 유지 + 분기 정리 | 기존 방식의 if-else를 상수로 추출해 정리 | 분기 자체가 남아 있어 확장할수록 복잡도 재증가 |
| 모노레포 + 패키지 분리 | 공통 로직을 npm 패키지로 분리 | 빌드 파이프라인 오버헤드가 현 팀 규모에 과도함 |
| **Config-driven 아키텍처** | 관리자 페이지에서 Feature를 ON/OFF, 코드 수정 없이 동작 | — |

Config 데이터를 앱 최상단에서 한 번에 주입하면, 하위 컴포넌트는 설정값만 읽어 조건부 렌더링을 수행하므로 비즈니스 분기가 코드 밖으로 이동합니다.

#### 해결 과정
- Initial Load 시점에 서버 API를 호출해 해당 프로젝트의 설정값을 일괄 수신하고 최상단 Config Context에 주입했습니다.
- 데이터 수신 전까지 메인 렌더링을 블로킹하되, 브랜드 로딩 스피너를 배치해 빈 화면 노출을 방지했습니다.
- 하위 컴포넌트는 \`useConfig()\` 훅으로 Feature Flag를 읽어 조건부 렌더링을 자체적으로 결정하도록 설계했습니다.
- 관리자 페이지에서 설정값 토글만으로 전체 동작이 달라지므로, 신규 파트너사 온보딩 시 프론트엔드 코드 배포가 불필요해졌습니다.

> 이 챌린지의 기술적 상세는 인사이트 **[Config-driven 아키텍처로 React 프로젝트 파편화 막기](/insights/config-driven-architecture-react)**에서 다루고 있습니다.

#### 결과
- 신규 호텔 온보딩 시 프론트엔드 코드 수정 건수: N건 → 0건
- 파트너사 추가 시 배포 없이 관리자 설정만으로 환경 구성 가능
- 공통 버그 수정이 전체 파트너사에 단일 배포로 일괄 반영

---

### 챌린지 2: 전역 상태 비대화(Provider Hell) 해결 및 캡슐화

#### 문제 상황
객실 선택 → 정보 입력 → 결제로 이어지는 복잡한 상태를 App.tsx 최상단에서 전역으로 관리했습니다. 예약 상태 하나가 바뀌어도 무관한 결제 컴포넌트가 리렌더링되었고, 결제 도메인 밖의 컴포넌트에서도 useContext로 예약 상태에 접근해 예기치 않은 부수 효과를 유발할 수 있었습니다.

#### 고려한 대안과 판단

| 대안 | 접근 | 기각 사유 |
|------|------|-----------|
| Redux Toolkit | 전역 스토어 중앙화, 강타입 슬라이스 | 보일러플레이트 부담, 소규모 팀에 과도한 복잡도 |
| Zustand | 경량 전역 스토어, 간결한 API | 도메인 경계 강제 불가, 어느 컴포넌트에서나 접근 가능 |
| **Context API + 라우터 레벨 격리** | 도메인별 Provider를 각 라우터에 배치, Custom Hook으로 접근 제한 | — |

라우터 레벨 격리는 해당 도메인 진입 시에만 Context가 마운트되므로, 별도 라이브러리 없이 스코프와 리렌더링 범위를 동시에 통제할 수 있다고 판단했습니다.

#### 해결 과정
- App.tsx에는 앱 구동에 필수적인 User·Config Provider만 남기고, 예약·결제 Provider는 해당 기능이 동작하는 개별 라우터 레벨로 이동했습니다.
- 라우터를 벗어나면 Context가 자동으로 언마운트되어 메모리가 정리되고, 관련 없는 컴포넌트의 리렌더링을 원천 차단했습니다.
- Context를 직접 노출하지 않고 useReservation 같은 Custom Hook을 통해서만 상태에 접근하도록 강제하여, 도메인 외부에서의 상태 변조를 불가능하게 만들었습니다.

> 이 챌린지의 기술적 상세는 인사이트 **[Context API 지옥에서 벗어나 라우터 레벨 캡슐화 달성하기](/insights/context-api-encapsulation-and-router-level-isolation)**에서 다루고 있습니다.

#### 결과
- 예약 상태 변경 시 결제 컴포넌트 불필요 리렌더링 제거
- 도메인 외부에서의 상태 변조 경로를 구조적으로 차단
- 컴포넌트 단위 테스트 작성이 용이해져 코드 예측 가능성 향상

---

### 챌린지 3: 크로스 플랫폼 결제 환경의 UX 보호 및 데이터 정합성 보장

#### 문제 상황
모바일 환경에서는 PC와 달리 PG 결제가 페이지 이동(Redirect) 방식으로 진행됩니다. 결제창으로 이동하는 사이 사용자의 입력 데이터가 유실되는 문제와, 복귀 시점에 재고·가격이 이미 변동되어 결제 금액과 실제 데이터가 불일치하는 두 가지 리스크가 동시에 존재했습니다.

#### 고려한 대안과 판단

| 대안 | 접근 | 기각 사유 |
|------|------|-----------|
| sessionStorage만 사용 | 탭 단위 임시 저장 | 브라우저 종료·새 탭 복귀 시 데이터 유실 |
| 서버 임시 저장 | 결제 전 상태를 서버 세션에 저장 | 별도 API·세션 관리 비용, 불필요한 서버 의존도 증가 |
| **localStorage TTL + 서버 중심 재조회** | 입력값은 클라이언트 캐시, 가격·재고는 복귀 시 서버 재조회 | — |

클라이언트 상태(사용자 입력)와 서버 상태(재고·가격)를 명확히 분리하면, 각자의 신뢰 소스를 올바르게 사용할 수 있다고 판단했습니다.

#### 해결 과정
- 사용자의 순수 입력값은 localStorage에 TTL을 부여해 캐싱하고, 결제 완료나 페이지 완전 이탈 시 즉시 파기하여 잘못된 데이터 복원을 방지했습니다.
- 결제창에서 복귀 시 가격·재고 등 크리티컬한 데이터는 반드시 서버 API를 재조회해 정합성을 강제했습니다.
- 복귀 시점에 재고·가격 변동이 감지되거나 파트너사 API 오류가 발생하면, 즉각 PG 승인을 취소하는 보상 트랜잭션을 실행했습니다.
- 단순 오류 메시지가 아닌 상황별 안내 문구와 함께 안전한 화면으로 리다이렉트해 UX와 데이터 무결성을 동시에 방어했습니다.

#### 결과
- 모바일 결제 복귀 후 입력 데이터 유실 건수 0건 달성
- 결제 금액·재고 불일치로 인한 이상 결제 차단
- 보상 트랜잭션을 통해 PG 이중 청구 없이 안전한 롤백 보장

## 3. 프로젝트 회고

- **플랫폼은 코드의 공유가 아니라 책임의 분리다:** 복제 기반 시스템이 진정한 플랫폼이 되려면 비즈니스 분기를 개별 컴포넌트가 아닌 Config 레벨에서 통제해야 한다는 것을 체감했습니다. 향후 SSR 환경을 도입한다면 Config 데이터를 서버 렌더링 단계에서 미리 주입하는 구조로 재설계가 필요합니다.
- **도구보다 설계 원칙이 먼저다:** Redux 없이도 Context API와 라우터 격리만으로 도메인 경계를 충분히 강제할 수 있었습니다. 상태 관리 라이브러리를 선택하기 전에 스코프와 접근 제어 정책을 먼저 설계하는 습관을 갖게 되었습니다.`
  },
  {
    slug: 'integrated-reservation-platform',
    title: '호텔 예약 · 결제 통합 플랫폼',
    description: '1인 백엔드 및 PM/PL로서 요구사항 정의부터 Core 아키텍처 설계, Optimistic Lock 기반의 동시성 제어, 인프라 트러블슈팅까지 전 과정을 주도하며 모놀리식 환경에서 최적의 성능과 데이터 정합성을 이끌어낸 통합 플랫폼 프로젝트입니다.',
    iconName: 'Server',
    category: 'Backend',
    techStack: ['TypeScript', 'NestJS', 'MySQL', 'Prisma', 'Jenkins', 'PM2', 'Cloudflare'],
    status: 'In Progress',
    apiCount: 0,
    overview: '1인 백엔드 및 PM/PL로서 요구사항 정의부터 Core 아키텍처 설계, Optimistic Lock 기반의 동시성 제어, 인프라 트러블슈팅까지 전 과정을 주도하며 모놀리식 환경에서 최적의 성능과 데이터 정합성을 이끌어낸 통합 플랫폼 프로젝트입니다.',
    period: '2025.12 – 2026.03',
    team: 'FE 3명 / BE 1명 (본인) / PM·PL 겸임',
    content: `## 1. 프로젝트 개요 및 역할

호텔 객실 조회부터 예약, 결제, 관리자 운영 시스템까지 아우르는 통합 예약 플랫폼을 1인 백엔드로 기획·개발 중인 프로젝트입니다. 고객사 요구사항 정의부터 Core 아키텍처 설계, 동시성 제어, 인프라 트러블슈팅까지 전 과정을 주도했으며, FE 3명과의 협업 속에서 PM·PL도 겸임했습니다. 비즈니스 확장성을 고려한 데이터 모델링과 결제·재고의 정합성 보장이 핵심 목표였습니다.

## 2. 핵심 챌린지 및 문제 해결

### 챌린지 1: 파편화된 데이터 구조 탈피와 Core Product 모델링

#### 문제 상황
기존 시스템은 상위 Product 개념 없이 테이블이 독립적으로 파편화되어 있었습니다. 주문 테이블이 FK 대신 "테이블명+id" 형태의 문자열로 데이터를 참조하고 있어, 주문 처리 로직 전반에 걸쳐 테이블 타입별 분기 처리가 하드코딩된 기술 부채 상태였습니다.

#### 고려한 대안과 판단

| 대안 | 접근 | 기각 사유 |
|------|------|-----------|
| 현행 유지 | 문자열 참조 구조 그대로 사용 | 분기 로직 증가로 신규 상품 추가 시 수정 범위 통제 불가 |
| 완전 정규화 | 모든 관계를 FK로 연결, JOIN으로 조회 | JOIN Depth 5단계 이상 → 조회 쿼리 복잡도·성능 저하 |
| **Core Product 다형성 + 전략적 비정규화** | 공통 부모 테이블을 두고 하위 테이블이 FK 참조, 재고는 객실을 직접 참조 | — |

Core Product 테이블을 공통 뿌리로 두고 type 컬럼으로 상품 종류를 식별하는 다형성 구조를 채택했습니다. 완전 정규화 대신 JOIN Depth를 최대 3단계로 제한하는 전략적 비정규화를 병행해 조회 성능을 방어했습니다.

#### 해결 과정
- 모든 상품 테이블이 Core Product의 PK를 FK로 참조하도록 스키마를 재설계해 주문 테이블의 문자열 참조를 완전히 제거했습니다.
- 최하위 재고 테이블이 중간 단계를 건너뛰고 객실 테이블을 직접 참조하도록 설계해 빈번한 재고 조회의 JOIN Depth를 줄였습니다.
- 신규 상품 타입 추가 시 Core Product에 레코드를 추가하고 하위 테이블만 생성하면 되는 구조로, 기존 주문·결제 로직에 변경이 없도록 설계했습니다.

#### 결과
- 주문 처리 로직 내 타입별 분기 코드 완전 제거: 조건 분기 약 15개 → 0개
- 신규 상품 타입 추가 시 수정 범위: 전체 주문 로직 → 신규 하위 테이블 1개로 격리

---

### 챌린지 2: 예약·결제·재고의 동시성 충돌 방어 및 정합성 보장

#### 문제 상황
실시간으로 감소해야 하는 재고와 외부 의존성이 높은 결제 API가 맞물리는 구조였습니다. 다수 사용자가 동시에 동일 객실을 예약 시도하거나, 결제 API 호출 이후 내부 트랜잭션이 실패하면 재고는 차감되었으나 예약은 미생성되는 데이터 불일치 상태가 발생할 수 있었습니다.

#### 고려한 대안과 판단

| 대안 | 접근 | 기각 사유 |
|------|------|-----------|
| 비관적 락(Pessimistic Lock) | SELECT FOR UPDATE로 재고 행 잠금 | 트래픽 집중 시 DB 락 대기 누적 → 처리량 저하 |
| 애플리케이션 레벨 큐 | 예약 요청을 순차 처리 | 외부 결제 API 실패 시 롤백 복잡도 급증, 추가 인프라 필요 |
| **낙관적 락(Optimistic Lock) + 원자적 트랜잭션** | version 컬럼으로 충돌 감지, 예약·결제·재고를 단일 트랜잭션으로 묶음 | — |

락 대기 없이 충돌을 감지하는 낙관적 락이 예약 시스템의 쓰기 빈도와 성능 요건에 더 적합하다고 판단했습니다. 예약·재고 차감·결제 검증을 단일 트랜잭션으로 묶어 원자성을 보장했습니다.

#### 해결 과정
- 예약 생성, 재고 차감, 결제 검증 세 단계를 Prisma $transaction으로 묶어 어느 한 단계라도 실패하면 전체가 롤백되도록 구성했습니다.
- 재고 테이블에 version 컬럼을 도입하고, 차감 시점의 version과 조회 시점의 version이 다르면 충돌로 판정해 즉시 롤백했습니다.
- 동시성 충돌 발생 시 단순 품절 메시지 대신 "잠시 후 다시 시도해 주세요" 응답을 반환해 재시도 가능성을 열어두었습니다.

> 이 챌린지의 HTTPS 환경에서 인증 정보 전송 방식에 대한 기술적 배경은 인사이트 **[구글과 네이버는 왜 비밀번호를 평문으로 보낼까?](/insights/https-and-plaintext-password-transmission)**에서 다루고 있습니다.

#### 결과
- 동시 예약 충돌 시 데이터 불일치 발생률: 재현 가능 → 0% (트랜잭션 원자성 보장)
- 결제 API 실패 후 고아 재고 차감 건수: 발생 가능 → 0건 (롤백 일원화)

---

### 챌린지 3: Cloudflare 봇 차단 트러블슈팅과 인증 라우팅 중앙화

#### 문제 상황
두 가지 이슈가 동시에 발생했습니다. 첫째, 도메인이 분리된 배포 환경에서 httpOnly secure 쿠키가 클라이언트에 전달되지 않아 로그인 세션이 유지되지 않는 현상이 있었습니다. 둘째, 관리자 API 경로가 늘어날수록 각 컨트롤러마다 인증 Guard를 선언해야 하는 구조로, 누락 가능성과 유지보수 비용이 높았습니다.

#### 고려한 대안과 판단

| 대안 | 접근 | 기각 사유 |
|------|------|-----------|
| 쿠키 → Authorization 헤더 전환 | JWT를 헤더로 전달 | httpOnly 쿠키 포기 → XSS 취약점 노출 위험 |
| Cloudflare 비활성화 | 리버스 프록시 우회 | CDN·DDoS 방어 포기, 운영 환경에서 불가 |
| **Cloudflare 봇 감지 설정 조정 + MiddlewareConsumer 일괄 적용** | 서버 간 통신을 봇으로 오인한 룰 수정, 인증을 라우팅 레벨에서 일괄 처리 | — |

쿠키 보안 속성을 포기하지 않고 인프라 설정을 수정하는 방향을 선택했습니다. 인증 중앙화는 Guard 개별 선언 방식보다 MiddlewareConsumer를 통한 경로 기반 일괄 적용이 유지보수에 유리하다고 판단했습니다.

#### 해결 과정
- 쿠키 유실 원인을 네트워크 레이어부터 역추적한 결과, Cloudflare가 서버 간 통신을 봇 트래픽으로 오인해 요청을 차단하고 있음을 확인했습니다.
- Cloudflare 보안 설정에서 내부 서버 IP 대역을 신뢰 범위로 등록해 차단 룰을 우회했습니다.
- NestJS의 MiddlewareConsumer를 활용해 /admin 하위 전체 경로에 JWT 검증 미들웨어를 일괄 적용하고, 로그인 등 인증 불필요 엔드포인트는 .exclude()로 명시적으로 제외했습니다.

> Middleware와 Guard 중 어떤 방식을 선택할지에 대한 트레이드오프 분석은 인사이트 **[\[NestJS\] Guard가 정답일까?](/insights/nestjs-middleware-vs-guard-tradeoff)**에서 다루고 있습니다.

> Next.js와 NestJS 간 도메인 분리 환경의 아키텍처 구성에 대해서는 인사이트 **[\[Next.js x NestJS\] 도메인이 다른 두 프로젝트를 연동하며 깨부순 아키텍처 오해들](/insights/nextjs-nestjs-domain-separation-and-bff)**에서 다루고 있습니다.

#### 결과
- 쿠키 기반 인증 정상화: 세션 유지 불가 → 전 환경 정상 동작
- 인증 미들웨어 누락 가능 경로: 컨트롤러 개수만큼 → 0개 (라우팅 레벨 일괄 적용)
- 신규 관리자 API 추가 시 인증 처리 추가 작업: Guard 선언 필수 → 불필요

## 3. 프로젝트 회고

- 정규화와 성능은 정답이 없는 균형의 문제임을 체감했습니다. Core Product 모델링 과정에서 완전 정규화를 포기하고 전략적 비정규화를 선택한 판단이, 이후 조회 API 개발 속도와 쿼리 단순성을 실질적으로 높여주었습니다.
- 프레임워크가 제공하는 도구를 맹목적으로 따르기보다 현재 구조의 맥락에서 트레이드오프를 따져보는 습관이 생겼습니다. Guard 대신 MiddlewareConsumer를 선택한 것처럼, 더 "올바른" 방식보다 지금 상황에서 유지보수 비용이 낮은 방식을 고르는 판단력이 실무에서 더 중요하다는 것을 배웠습니다.`
  }
];
